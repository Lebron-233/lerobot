# F-PTI1精确接续：接口已通过，先补齐数据合同再固定同预算微调

执行30e495c3；5次真实模型forward/grad与独立CPU审计完成，不重跑。原数据meta错误映射已定位并对episode814跨来源核验175行。数据packet及所有报告保留；旧Graph/RTC/任务保持阴性不改写。

## 已有可复用内容

`smolvla_prefix_training.py`提供逐动作时间、干净前缀、终端右padding和合法后缀损失。C0完整输出/loss及8个投影参数梯度相对原路径exact；参数未更新。其余训练控制、checkpoint模式与条件采样尚未完成，不能把该函数当成已训练好的线上策略。

## 下一步单一主线

1. **固定数据合同而非继续猜FPS**。以已修正索引的lerobot/libero@a1aaacb7f6cd6ee5fb43120f673cebb0cfea7dd4为候选索引来源；逐episode核对实际文件的episode/frame/global/task字段。原HuggingFaceVLA/libero@86958911的meta不可直接复用；图像必须与真实数值行、相机顺序逐条绑定。核实所选任务原始演示来源/转换脚本、动作单位、控制步长和图像变换；10FPS时间轴不自动等于20Hz物理兼容。必要时取得匹配的原始HDF5/RLDS元数据再比对；在这一步没有证据前不做动作插值、降采样或改标签。

2. **在看微调结果之前按完整轨迹划分**。明确选择本地4任务对应的语言/数据task ID，不能将不同仓库数字ID混用。排除本次已见episode814作为开发探针；对其余轨迹固定哈希排序与训练/开发/封存比例，报告每组数量和重叠为0。只声明相对本次微调独立，不声称基座预训练从未见过；仿真新初态的独立性另行查重。

3. **一个同预算普通微调对照和一个前缀条件候选**。在运行前固定可训练模块、优化器、更新数、种子、C分布、batch、padding/有效损失分母。普通臂与前缀臂共享数据和预算；不能把普通续训收益归因于前缀。先明确前缀监督只来自正确演示，尚不能覆盖旧策略错误前缀；不得用旧失败rollout或自身输出冒充专家纠正。

4. **先补齐采样/检查点语义，再解释闭环结果**。新模型需要与训练一致的干净前缀/逐动作时间条件采样，默认关闭，显式新checkpoint接口版本。原guided RTC不能代替该训练式采样；不得将当前SmolVLA配置简单切成trained或偷换成Pi05。新权重必须重新检验其eager/Graph输出等价及原20Hz/350ms完整请求预算，不继承旧检查点资格。

5. **冻结后再做有界真实反馈**。保留原时间原点和实际消费一次trim、全部失败身份；开发门通过才独立确认。未解决任务保持时不添加无界调参，不按7/18和7/19身份设置特例。

## 当前执行边界

本轮正式optimizer更新0、Env0、Graph capture0；仅5次完整真实模型前向和梯度诊断。154CPU回归通过、1旧CUDA条件跳过。单一演示没有正式train/dev/sealed划分，完整物理时间合同未接纳。不将5个不同C的loss比较当成任务收益，也不将训练forward时间当部署时延。

后继先读RESULT和本轮result/audit/receipt，不重新prepare或发布同一登记。原21pending保留。必要原分支和Issue1发布授权沿用，明确安全权限拒绝停止对应动作；普通网络错误先核实状态，不盲重POST。不自动真机、生产默认更改、依赖升级或长期无人值守任务。
