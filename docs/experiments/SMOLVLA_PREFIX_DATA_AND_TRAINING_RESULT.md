# F-DTC1 / F-PTP1：原始行时间合同与真实前缀条件微调

日期2026-09-22。本轮从7f12fa6d接续，实际完成两条有界新实验，而非复读旧结果。数据回放执行86c6af436fc5c25dd83faf75847e839e0d53f417，微调执行24c7ed49c6f05299991e605b7b92a21721c69e09。两个正式运行与独立CPU审计均退出0；原Graph/控制器/模型源权重及21份旧pending不变。

## 核心结论

1. 原始175行动作的20Hz回放明显更符合数据：位置逐坐标RMSE0.387mm且完成；10Hz为98.688mm且未完成。支持保留原始物理动作行和20Hz解释，不应依据转换存档10FPS标签重复、插值动作或降低原控制频率。不是说旧异步失败由频率造成；旧控制器本来就是20Hz。
2. 已锁定原始RLDS来源并冻结四任务12train/4dev/4sealed。既有175行milk接口诊断轨迹排除。完成16个train/dev包及254幅图像的原始字节解码逐值检查。没有把sealed图像/模型指标用于训练。
3. 同预算ordinary/prefix各128次更新真实完成。前缀模型在相同正确前缀输入下较ordinary误差降低3.504%，4/4开发轨迹均改善；但其正确前缀误差仍比ordinary无前缀高2.316%，仅1/4轨迹胜出。预定离线推进门未通过，不晋级或替代冻结候选。

## 1. 数据来源与时间核查

原始源：openvla/modified_libero_rlds@6ce6aaaaabdbe590b1eef5cd29c0d33f14a08551，libero_object_no_noops/1.0.0。固定读取首4分片，逐条核验长度与payload CRC32C，结构共57episode。无需安装TensorFlow；自建只读Example解析器经过腐损/边界测试，不执行下载代码。

首分片record7与上一轮HuggingFaceVLA/libero转换episode814的全部175行state/action逐值相同。双相机经PIL解码并非逐像素相同（0/175exact，最大差17和20），没有把这一差异假定为无害或擅自旋转图像。本轮微调使用同一原始RLDS记录里的图像与状态/动作，不混配转换后索引或镜像状态。

固定clip-rt/modified_libero_hdf5@6a6659f8ac7d580fd594173a0e3abf880c843130的milk demo_32，175行动作转换float32后与RLDS全等；状态前25行相同，其后最大坐标差约0.004132。镜像的sim state中连续时间差为0.05秒，第0行保存的是初始化状态，不能把其到第1行0.55秒间隔当控制周期。以约5.37MB有界HTTP Range读取完成核查，未下载整份2.54GB文件。

固定上游源码：openvla c8f03f48af692657d3060c19588038c7220e9af9，LIBERO 8f1084e3132a39270c3a13ebe37270a43ece2a01，openpi 215abfb217dbac7d5f1273282331b9b1866c0479。再生成代码先跳过no-op，否则记录并执行一次动作；LIBERO环境默认20Hz。转换脚本逐条写RLDS却指定fps10。原始RLDS没有timestamp字段，因此不能仅凭转换后0.1秒时间戳断定物理动作来自10Hz。

## 2. F-DTC1：固定动作、两种物理步长的实测

真实预登记5771629057。相同完整sim initial state、seed0，各10次空动作稳定后，固定顺序20Hz再10Hz；各175个相同原始动作，不插值、不重复，不在看到成败后更换初态。

|量|20Hz|10Hz|
|---|---:|---:|
|记录动作|175|175|
|实际sim时钟差|约0.05s|约0.10s|
|8D状态MSE|0.000000589830493|0.046763784623|
|末端位置逐坐标RMSE|0.000387082612m|0.098688431526m|
|与存档完全相同的状态行|26/175|0/175|
|终局原生成功|是|否|

位置指标是对175×3个坐标误差取均方根，不是每帧三维欧氏距离的RMSE。8D状态含位置、姿态和夹爪量，不给混合MSE附加单一物理单位。20Hz状态MSE/10Hz=0.0000126129760075。

重要限制：10次稳定动作在20/10Hz下分别耗0.5/1.0秒物理时间，测量首状态不逐值相同，最大坐标差0.0009511034。因此是完整来源回放配置比较，不证明某一个细微物理效应是唯一原因。只回放一个已见演示，不能当作VLA任务成功率或全四任务逐轨迹物理认证；raw audit保留full_four_task_training_ready=false。

本实验创建/关闭2Env，20settling、350正式回放动作，模型前向/优化器/Graph均0。运行约9.615秒（非真实时间调度基准）。独立CPU审计重新计算350命令、时钟差、状态MSE/位置RMSE和两个终局，未重新执行物理仿真。

## 3. 四任务数据划分与用途

四任务按明确语言映射为本地task0字母汤、2沙拉酱、6黄油、7牛奶，不混用转换数据的数字任务ID。完整轨迹标识由来源路径、状态和动作内容hash构成，另核验20条动作序列均唯一。排除旧175行milk诊断，余20轨迹按固定hash排序：每任务首条dev、第二条sealed、其余train。

|任务|train|dev|sealed|
|---|---:|---:|---:|
|字母汤0|5|1|1|
|沙拉酱2|1|1|1|
|黄油6|4|1|1|
|牛奶7|2|1|1|
|合计|12|4|4|

manifest SHA256=d1a60540fce2056d10c29ea361329da2730e2e5f89415f86731c9e57c68d7bbf。没有按帧随机划分；sealed只作结构/数值哈希/去重核查，图像未解码也未运行模型。16个train/dev包的state/action与源记录全等，127个物理行anchor的254幅相机图像均与源解码全等。沙拉酱仅一条训练轨迹，严格定位为小规模离线数值学习试验。既不声明基座预训练未见，也不声明完整训练数据的物理复验资格。

## 4. F-PTP1：同预算、同后缀目标的两个真实微调

真实预登记5771750593。ordinary和prefix均从原始检查点恢复开始，仅四个动作投影模块的723392参数（8张量）可训练，冻结视觉/语言/Transformer专家参数但允许梯度穿过计算图。各128更新/batch1，AdamW lr2.5e-6、betas(.9,.95)、eps1e-8、weight_decay0、clip1，固定原始数据、anchor、noise、C和流时间schedule；每任务32步，无中途验证选模、无学习率或C搜索。

ordinary所有动作输入正常加噪；prefix前C行为干净输入且每动作流时间0，后缀正常加噪。两臂监督相同的C之后合法7D后缀，分母逐步完全相同。因此ordinary是同后缀监督的无条件续训对照，不冒充默认全50行BC。流时间表征噪声，不是物理控制时间。

评价frozen、ordinary末态、prefix末态，各自无条件/正确专家前缀/错配前缀。4条dev×2个固定中段anchor×C3/8=16case，各3mode×3checkpoint共144评价。正确前缀属于离线专家条件，线上由旧策略生成的前缀可能不同。评价目标是归一化流速度后缀MSE，不是十步积分后的最终动作MSE。

|检查点/输入方式|无显式干净前缀|正确专家前缀|错配前缀|
|---|---:|---:|---:|
|原始冻结|0.321690794|0.343489458|0.448347417|
|ordinary末态|0.323490898|0.343003648|0.447547648|
|prefix末态|0.323021435|0.330983282|0.427943437|

按轨迹等权，各列同16case目标和mask。相同正确前缀下prefix比ordinary低3.504%，比原冻结正确前缀低3.641%；但与ordinary自身无条件配置比高2.316%，与原冻结无条件比高2.889%。prefix正确比自身错配低22.657%，然而原冻结和ordinary也能区分正确/错配，不能把这一差别全部归于本次训练。

|dev任务（各一条轨迹）|ordinary无前缀|ordinary正确前缀|prefix正确前缀|
|---|---:|---:|---:|
|字母汤|0.410854093|0.432535151|0.425837946|
|沙拉酱|0.240133957|0.283974428|0.264883175|
|黄油|0.373717017|0.392401548|0.383947367|
|牛奶|0.269258526|0.263103463|0.249264641|

同正确条件下4/4改善，但主配置比较只有牛奶1/4改善。四项预定离线探索门：优于ordinary无条件=false；优于冻结正确前缀=true；至少3/4轨迹优于ordinary=false；正确优于错配=true。因此offline_followup_supported=false，不能仅挑同条件列宣布主比较通过。

## 5. 本轮实际推进与不能宣称的内容

时间语义获得实测支持，数据源索引陷阱被避开，完成固定整轨迹划分，前缀微调从只有5次梯度检查推进到256次真实参数更新。输入方式下的开发误差有改善，但尚未超过更简单的无前缀参照；没有新增可部署候选。

仅调整投影层、仅12训练轨迹/4开发轨迹、仅128步，也未验证全专家可训练优化器。本轮既不能证明训练路线不可能，也不能证明加大训练必然解决任务保持。四条dev不是144个独立样本。未读sealed模型指标，不增加训练、未运行条件十步采样、未重做新权重Graph等价，也没有VLA生成命令控制环境。旧E-GFC1和RTC闭环阴性保持。

## 6. 执行与证据

F-PTP1完整forward400（训练256+评价144）、backward256、两臂优化器更新128/128，一次VLA加载。全部8投影张量均发生真实更新，两个optimizer最后step128；非投影权重hash保持。两个独立检查点保存，进程结束前原始投影参数和求导标志恢复。运行44.431秒、峰值allocated 1566045184字节，这不是部署时延/冷启动/吞吐基准。

独立CPU审计重新归约144个保存的velocity/target/mask损失，128对训练目标和分母hash一致，检查checkpoint/optimizer最后计数和源哈希。它没有重算Transformer、参数梯度或所有中间AdamW更新。51份微调来源、21份数据回放来源、所有原21pending保持；35项最终相关CPU测试通过（含原前缀16项），Ruff通过。旧代码未改。

正式F-DTC1、F-PTP1各attempt1/retry0，无技术首错。开发数据探查中一个不存在元数据URL404，后续打印该文件的诊断命令报FileNotFound但已完成的原始record清点保留；导入排序检查先失败后修正。GitHub连接器一次旧提交只读fetch被环境FORBIDDEN拒绝，未重做该读取；两个新实验的正常提交、push、预登记和真实ID回读成功。不能把这些开发/连接器问题隐藏成全程没有错误，也不将它们当成正式实验科学失败。

原件目录：outputs/smolvla_demo_time_86c6af43，outputs/smolvla_prefix_train_pilot_24c7ed49，outputs/smolvla_prefix_data_contract_development_20260922/cohort。正式result/audit、数据manifest和描述性统计均逐字复制到docs。结果发布状态以最终publication_receipt.json为准。后续见SMOLVLA_PREFIX_TRAIN_PILOT_NEXT.md。
