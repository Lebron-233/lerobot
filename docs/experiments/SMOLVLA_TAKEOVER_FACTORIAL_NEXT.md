# E-TFD1之后：接管诊断的完成边界与后继问题

本轮8格全部运行并审计通过，原4条校准轨迹逐值重现。7/19仅00成功、20/02/22均失败；7/18四格成功。E-GFC1未通过结论、候选与权重保持冻结。

## 已排除的简单修法

不能仅在第一次接管等待：02仍失败。不能认为第一次延迟之后全部等待能恢复：20仍失败。也不能将两步延迟当作普遍安全或危险阈值：邻近7/18仍四格成功。线上时延满足预算与动作策略对接管的容忍性是不同的问题。

## 后继建议，尚未执行

优先研究全重规划过程的前缀一致性，不继续盲目加速、改学习损失或只保护首个请求。先在原开发数据上固定度量：续执行旧块与当前新块同一动作时刻的差异、接管前后末端状态和动作变化。只作诊断，不据已选失败例寻找部署阈值。
下一份仿真反事实合同应显式区分“执行旧前缀改变物理状态”与“跳过新块开头行改变后续计划”的影响，保持模型冻结、固定干预点和共同起点校准。任何不按原时间身份取行的分支只能作为非部署诊断，不能伪装为原算法的正确性修复。
在确定可操作的前缀一致性方案后，独立建立新候选版本并验证完整路径成本。不能把本轮人为等待获得的成功当作保持异步重叠或延迟预算的证据；未经测量不能宣称RTC+Graph已经可实时。

## 数据与停止规则

7/18、7/19均已作为开发诊断使用；本轮不是新增资格，结果不加入32对确认。旧确认31/32对32/32及主门未通过保持原样。后继选样、预算、主要判据与独立确认身份须在新执行前固定，不能测到阳性为止。
本轮唯一attempt1/retry0结束，worker已收回，原输出和准备目录不覆盖。保留21份旧pending，原候选锁不更改。未启动下一份诊断、新训练、RTC试验或真机运行。
