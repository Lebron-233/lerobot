# RTC-FULLPATH1：真实RTC正确运行，但当前完整路径不满足固定延迟预算

2026-09-17。阶段二已完成全部192次真实SmolVLA请求，并通过独立CPU审计。不是新的闭环试验；第三阶段未启动。

## 身份与状态

- execution HEAD：92c3981234e85d97f0ba398db071746b4376162a。
- 预登记实际评论：https://github.com/Lebron-233/lerobot/issues/1#issuecomment-5707916459。
- 原件：outputs/smolvla_rtc_fullpath_92c39812；准备：outputs/smolvla_rtc_fullpath_preparation_92c39812。
- preparation.json SHA256：876eaa186b7b30ee429db19c5f040989ba8a5bc6c96a19041328fa6beadb73bf。
- result.json SHA256：aa67a69757da22b8ea943033ee343cc6e284b984886047b530d40dba28fa149e。

status=completed；independent_contract_accepted=true；latency_gate_passed=false。
完整调用有技术接纳，不代表满足实时预算、闭环成功或真机安全。旧未来视觉预测补偿阴性结论不变。

## 本轮改变了什么

将上一轮已完成的RTC Jacobian记录、缺失动作维度掩码和观测时刻原点队列代码正常提交并推送；没有改变那些已通过测试的源码字节。本轮提交9文件，未重做或重复计入历史267项测试。
此前仅有合成正确性；本次实际运行冻结SmolVLA和修复后的guided RTC。VLA权重没有更新，guided输入求导不是训练。

只使用原F-COV1 task6/7、state48/49、request3..6的16个开发样本；原始双相机、状态、语言及噪声保持。前缀为观测时刻实际旧请求row20起的30x7归一化动作尾部，旧承诺前三行核对一致。
base和RTC两臂均eager、chunk50、十步解码；RTC固定EXP、execution_horizon10、max_guidance_weight10、guided。没有Graph、学习视觉残差、trained RTC或参数搜索。
每例每臂6次，repeat0无前缀，repeat1..5按冻结顺序交替。总192次；每臂80次后续计时，首个真正guided调用保留其中，不增加预热或剔除慢调用。
16例反复5次不等于80个独立机器人场景；数据仅是开发成本测量，不是新的性能留出集。

## 完整请求时延

计时含原始观测转换、预处理/搬运、真实视觉编码、prefill、动作解码/RTC VJP、后处理和CPU输出，CUDA同步封口；两臂都含观察用证据复制，磁盘写入在计时外。queue_wait_s=null，无线上排队或环境重叠测量。

| 指标 | 无RTC base | guided RTC |
|---|---:|---:|
| 后续计时请求数 | 80 | 80 |
| 平均完整请求 / ms | 280.199 | 660.013 |
| P50 / ms | 279.963 | 659.936 |
| P95 / ms | 291.899 | 672.867 |
| P99 / ms | 292.692 | 731.916 |
| 最大值 / ms | 292.692 | 731.916 |
| ceil(P99*20)+1 | 7 | 16 |
| 是否满足cap8和一行动作余量 | 是 | 否 |

分位数采用nearest-rank；80个观察下P99恰好取最大值，不能解释成已精确估计总体P99。
固定条件等价于完整请求P99<=350ms。RTC是该阈值的约2.09倍；平均完整时延是base的2.356倍。没有降低频率、扩cap、改guidance或修改判据。
这不是宣称RTC原理不可实时，而是当前硬件、权重、十步eager实现和观测性计时开销下不满足此预算。

### 开销主要在哪里

| 嵌套计时分量均值 / ms | base | RTC |
|---|---:|---:|
| 预处理与输入搬运 | 0.795 | 0.846 |
| 双相机视觉编码 | 12.053 | 12.096 |
| prefix prefill | 26.255 | 25.994 |
| flow matching（RTC臂含引导求导） | 237.614 | 610.046 |
| 公开predict_action_chunk总时长 | 279.257 | 658.885 |
| 后处理与输出搬运 | 0.147 | 0.281 |

完整路径平均增加379.814ms，其中flow matching计时增加372.432ms，占额外开销的98.06%。这是相同路径的描述性分量差分，不是将嵌套子项与公开总时长重复相加。
因此优化重点应是带引导的十步动作解码/求导，而不是相机读取或文本预处理。本轮没有拆分每个VJP算子耗时，不能把上述差值全归给某一个算子，也不能据此承诺某个编译方式的收益。

## 引导是否实际生效：事后保存输出分析

独立正式审计结束后，只用保存的输出与原始前缀做描述性CPU归约，新增模型前向/Env为0，不新增或修改推进判据。
衡量新块前3行、7维归一化输出对旧承诺前缀的MSE，16个样本各重复5次：

| 条件 | 对旧承诺前三行的平均MSE |
|---|---:|
| base | 0.290375341724302 |
| RTC | 0.002867557913174 |

平均下降99.0125%。各样本的5次完整输出在本臂内逐值一致；重复调用不是新的独立效果样本。
这说明修复后的真实RTC确实改变了前缀贴合，不是仅打开配置却没有引导。但这是对旧动作的一致性，不是专家准确率、接管动作质量或闭环成功率。
本测量输入inference_delay固定为历史3；实际RTC尾延迟需要16步，二者不兼容。不能把对前3行的贴合拿来声称可以安全覆盖16步实际延迟。没有用这项事后指标选择参数。

## 调用、退出和独立审计

| 项目 | 实际 |
|---|---:|
| VLA加载 | 1 |
| 完整请求 started/returned | 192/192 |
| 真实视觉编码 | 192 |
| 输入一致性检查 | 192 |
| 无引导完整50x32输出exact | 112 |
| 无前缀两臂配对exact | 16 |
| RTC包装步骤 | 960 |
| 有前缀VJP返回 | 800 |
| phase started/returned | 193/193 |
| 新训练更新/Env/资格读取/Graph/真机 | 0 |

正式Job：7b812b4e-db85-4dcd-9690-db52a43f7abe；worker678227。worker和外层均exit0，/proc/678227不存在。
监督UTC2026-09-17T03:16:27.195526至03:18:05.020540，wall97.825058180秒；first_failure/stop_reason=null，pending/active为空，无强制终止，attempt1/retry0。
独立审计Job816784ba-581f-4169-9752-c3a9b6a8bfeb，exit0。逐一核验192记录、112无引导输出、16无前缀对照、分位数与固定门、193阶段及退出证据；CUDA未初始化，未重新执行模型、图像编码或VJP。
输入exact计数由冻结runner路径提供，CPU审计不从原RGB重新计算token；不能把审计说成独立重新证明全部模型计算或GPU计时真实性。

## 三阶段当前结论及下一步

阶段一：源码与合成测试已冻结推送，阶段二进一步完成真实模型无前缀一致性和有前缀调用。
阶段二：完整实验技术接纳，延迟门失败。
阶段三：原24episode三臂合同的前置条件不成立，没有启动、不得冒充完成或重新解释为通过。

后继必须分别处理：无RTC工程基线的闭环验收，以及guided RTC的同公式实现优化。无RTC的当前完整P99满足预算，支持另行设计同eager两臂小规模闭环；不等于该闭环已经通过。
RTC优化必须保留完整Jacobian、合法维度掩码、十步/同权重/20Hz及cap8等比较条件，先做独立正确性与开销对照。不得把漏Jacobian的旧路径称为同算法加速；减小解码步数或引导强度只能列作另一个算法对照，不与同配置提速混为一谈。
未新增候选训练、资格采集、时延补测或仿真。本次成果是从合成正确性推进到真实模型的可复核计算边界。

## 工具状态与发布边界

本轮GitHub连接器最新评论GET实际返回：ToolError: FORBIDDEN: This conversation is restricted to developer MCPs。没有重做该读取或声称已获取最新评论。
本轮原生WebCodex git commit和非强制push成功；阶段二必要预登记经独立gh结构化POST一次及实际ID GET一次完成，body逐字一致，存为规范化投影及来源说明，不冒称原始HTTP捕获。
没有新git提交安全拦截；不得把上一轮拦截描述成本轮错误。用户授权已存在，不重复请求授权。
结果发布的真实HEAD/评论ID在独立出版回执中记录；本报告不会提前虚构它们。旧21份pending保持，生产资格与旧阴性不变。
