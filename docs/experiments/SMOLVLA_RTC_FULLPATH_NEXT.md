# RTC完整路径之后的执行分流（待执行）

2026-09-17，依据RTC-FULLPATH1执行HEAD92c39812的192次请求及独立审计。本文不启动新任务，不放宽原推进门。

## 当前决定

保留原阶段二结果：base P99=292.692ms，RTC P99=731.916ms；20Hz/cap8/一行动作余量要求<=350ms。原阶段三24episode三臂试验不具备前提，保持未启动。
停止继续微调未来视觉预测器；IQ1/ACQ资格集仍封存。异步执行与RTC增量各自验收。

## A：无RTC的异步执行工程闭环

后继可另立两个同eager实现的对照：serialized当前视觉、async当前视觉。不以回到旧row0时间约定换取更好结果；两臂使用观测时刻原点、新块按实际已消费动作只trim一次的同一队列。
拟4任务(0,2,6,7)乘2初态(拟8/9)乘2臂=16episode。身份仍未查重或冻结；正式采集前查全仓库身份、有冲突重新审阅而非注册后替换。配置20Hz、chunk50、十步解码、cap8、一行时延余量、阈值30保持；另行冻结准确种子、顺序、终止/预算及输出。
先实现共享worker和动作来源日志，并以合成线程/行号/episode关闭用例回归；合成队列不等于已完成真实控制器集成。
真实对照需确认配对初态、每条实际命令的chunk/row/observation身份、仅一次trim、异步模型与环境区间交叠、全部线程退出。记录全部success/timeout/技术失败、控制wall、无动作时隙、underflow、deadline miss及完整请求分位数；不补坏例或只报告成功。
这是一项新合同，不是悄悄把原24个三臂试验减为16个后宣称原计划通过。无RTC阶段二过延迟门只是开展该工程试验的依据，不是闭环成功。

## B：guided RTC的实现优化

先以合成解析denoiser、当前真实调用存档和代码审阅，检验可保持公式的执行/缓存/编译方式。完整VJP已验证不可省略；不能重新移到前向后requires_grad或将缺失维度视作零目标来换取速度。
性能目标是在同一测量定义下P99<=350ms；当前需约2.09倍尾部加速，这只是预算差距，不是可实现性承诺。
优化不得改原权重、十步解码、EXP/horizon10/guidance10、20Hz/cap8或固定比较的输入语义。若改步数或guidance，必须另标算法变化并验证动作质量，不能称同实现优化。
以新HEAD和独立预登记复测，保留原192次全部数据，不反复运行选最小P99；只有新技术/时延门通过，才重新设计A/B/RTC的三臂配对。也必须处理实际inference_delay与当前历史3步输入不一致，而非只测旧3步前缀。

## 边界

A、B尚未执行，本文不扩展既有实验预算。所有新执行先定义可验证实现、准确预算、唯一预登记和停止规则；旧输出不覆盖，无强制推送、reset/clean/stash或资格泄漏。明确安全拒绝不通过更换工具绕过，普通接口错误据实最小修复。
