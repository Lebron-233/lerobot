# E-TFD1：首次与后续接管延迟的反事实诊断结果

## 结论与身份

本次从既有准备目录接续，完成固定8个episode和一次独立CPU审计。四条完整校准轨迹逐值重现原存档，冻结候选不变。
在已知失败身份7/19上，只有全部等待（00）成功；仅第一次延迟两步（20）、仅后续延迟两步（02）、全部延迟两步（22）均失败。相邻成功身份7/18的四种条件全部成功。
因此，在本次固定身份、种子和干预下，首次接管延迟单独就足以使终局由成功变为任务时限失败；消除首次延迟也不足以抵消后续重复延迟的影响。不能把问题只归为首次接管或只归为后续累积，也不能以“最多两步延迟”作为普遍任务保持保证。
这是一项可解释的已见案例诊断，不是新独立确认、部署修复或总体成功率估计。E-GFC1的串行32/32、异步31/32和确认未通过结论保持原样。

- execution HEAD：787ad7e6413669033b4ab1851e50d6edda9da2d0。
- 冻结候选HEAD：a5e9fddefc0cfa03ea1509493e8b304ec7c06d92；核心实现、权重及旧控制器字节未改。
- 预登记实际评论ID：5716563147；单次POST，实际ID独立GET，正文exact存证后启动。
- PREP：outputs/smolvla_takeover_factorial_preparation_787ad7e6。
- preparation.json SHA256：662a88beb53640cb38c55f62c695c310f209cb35aaf2a6e0db62cfd877465fcc。
- 原件：outputs/smolvla_takeover_factorial_787ad7e6。
- result.json SHA256：ddb06f991a5f917397360fff5424cc08d1712ce78d387a864f6e532064240fec。
- independent_audit.json SHA256：72e5c6a59b9a1a9e90b609c42cce12382dc8839209344dea14f910b2e323f353。

## 固定干预，不是重新调整模型

两个身份都属于已用确认数据的开发诊断。7/19是原唯一回退案例；7/18是相邻同任务成功案例。四格各一次、共8个episode，未新增或替换身份，未搜索种子。
00/22/02/20的第一位表示request2第一次重规划安装前执行多少行旧动作，第二位表示request3及之后每次安装前执行多少行旧动作。bootstrap始终0。
控制器必须先消费规定的0或2行旧动作，再等待计算完成并安装；不因等待推进物理动作索引，新块依旧按实际已消费行数只裁剪一次。真实当前观测和新noise产生新动作控制环境，不用存档命令驱动干预。
复用原GraphFeedbackPredictor、InferenceOwner、RTCExecutionQueue、NativeSession及提交/延迟估计函数，保持50行、十步解码、20Hz、cap8、余量1、guard2、threshold30、P90/window50、初始delay7。未加入RTC、学习式视觉补偿或训练。
人为约束接管时序可能增加等待，因此本轮控制wall和请求时间只作为执行成本，不作为线上性能资格或新调度收益。

## 全部8格结果

| 身份 | 首次旧动作行数 | 后续旧动作行数 | 条件 | 结果 | 实际动作 | 正式请求 |
|---|---:|---:|---|---|---:|---:|
|7/19|0|0|全部等待|成功|190|10|
|7/19|2|2|全部两步|任务时限失败|280|14|
|7/19|0|2|仅后续两步|任务时限失败|280|14|
|7/19|2|0|仅首次两步|任务时限失败|280|14|
|7/18|2|2|全部两步|成功|141|8|
|7/18|0|0|全部等待|成功|141|8|
|7/18|2|0|仅首次两步|成功|142|8|
|7/18|0|2|仅后续两步|成功|143|8|

所有失败均为280步正常TimeLimit，不是技术错误。全部条件underflow=0，没有过期块安装。正常失败没有触发提前停止，8个预定条件全部执行。
7/19的20对00比较支持本条件下首次两步延迟足以改变终局，即使以后全部等待也未恢复成功；02对00则说明只保护首次接管仍会失败。但02同时改变所有后续接管，不能推论其中任意某一次后续接管单独足以致败。
7/18四格均成功，说明同样两步干预并非在相邻案例上都导致失败；不能将2解释为通用不安全阈值，也不能从该成功例反推通用安全阈值。
按成功0/1计算，7/19的交互对比s22-s20-s02+s00=1，这是二值结果对比，不能解释成积极协同或性能改善；7/18的所有对比为0。每格仅一个固定实现，不给总体效应置信区间。

## 为什么可以解释这次案例内的时序影响

独立CPU审计验证四条00/22校准轨迹，要求整条轨迹的实际命令、动作来源行号、物理状态与图像、全部8项模型输入、noise、完整及后处理输出、动作数和终局均与原存档exact。结果不是仅重现最终成败，而是完整轨迹逐值一致：

| 本轮ordinal | 原E-GFC1 ordinal | 身份/条件 | 命令exact | 物理观测exact | 请求exact |
|---|---:|---|---:|---:|---:|
|0|56|7/19，00|190|191|10|
|1|57|7/19，22|280|281|14|
|4|54|7/18，22|141|142|8|
|5|55|7/18，00|141|142|8|

此外，8个条件各自前21帧物理状态/图像，以及request1和request2的全部输入与输出均与同身份原串行起点exact，共16份共同请求。每次实际安装的旧动作消费数均符合预登记0/2干预，没有以墙钟延迟猜测剂量。

正式审计后另做只读CPU保存数组比较，没有新增模型或环境：
- 00与20在两身份上第一次命令分歧均为0-based action_index20。此时两臂request2的8项输入、noise和完整输出仍exact；00使用request2/row0，20继续request1/row20。
- 00与02第一次命令分歧均为action_index40。此时request3的8项输入、noise和完整输出仍exact；00使用request3/row0，02继续request2/row20。
这些结果加强了“不同执行时序使用相同预测结果而改变后续反馈”的解释，不是Graph数值差异的证据。
但干预同时改变旧动作续执行、新块已过去行的跳过及后续反馈轨迹，尚未分别识别这三者的作用，更未确定抓取或接触的唯一物理机制。没有证明对任意初态、噪声或延迟都成立。

## 真实成本、退出与独立审计

| 项目 | 实际完成 |
|---|---:|
| VLA加载 | 1 |
| 正式完整请求/真实双相机编码/Graph replay | 84/84/84 |
| 新Env/settling/measurement | 8/80/1597 |
| Graph bootstrap capture | 8 |
| 额外内部sampler：setup/warmup/capture | 8/24/8 |
| intent/return闭合 | 3414 |
| phase started/returned | 10/10 |
| 同owner释放Graph、恢复sampler、join并关Env | 8 |
| 正式attempt/retry | 1/0 |
| 训练更新/RTC引导/真机 | 0/0/0 |

1677次底层native step含80settling和1597measurement，外层1597次environment_step为同一推进包装，不重复计动作。
8次捕获准备合计11.126397秒，最大1.660206秒；startup范围1.382187到2.100900秒，均在控制开始前。40次额外sampler调用没有隐入正式请求数。新环境实例是两个已见身份的干预，不是8个独立新任务样本。
7/18的22条件request8在episode结束后完成，被队列禁止发布；75.194ms仍计入84次正式请求，属于正常停止取消，不是漏记或技术失败。
正式Job：5a0db70f-efc9-4952-82b6-07dac9779325。独立审计Job：2ef91395-af8c-460c-bf7b-1010c9d4b32e。
worker779797、外层和审计均exit0；UTC2026-09-17T15:03:44.835600至15:05:47.805627，监督122.970045秒。无首错、stop_reason、pending/active或强制终止，worker已退出收回。
726份来源/源码哈希、原21份pending状态及内容保持，候选锁与E-GFC1结果不改。原48项CPU回归及Ruff依据上轮已冻结记录，本次没有重复或计为新测试，没有重做prepare。
独立CPU审计不重新执行视觉编码、策略、后处理、物理仿真或GPU计时；它核验保存的轨迹、输入与输出一致性、干预剂量、动作来源、完整校准和退出证据。不能把它表述成全部模型计算的第二次独立运行。

## 本次接续与工具记录

上一轮创建registration.md的安全拒绝是历史事件。本次同一WebCodex.apply_text_edits正常创建该文件，原生gh单次POST和真实ID独立GET成功，登记与原准备文件绑定。没有换写入工具规避该拒绝，没有重复正式实验。
GitHub连接器读取执行提交时返回：ToolError: FORBIDDEN: This conversation is restricted to developer MCPs。没有重做这项被拒读取。必要的预登记查重、发布及回读是不同的已授权操作。
gh查重第一次因--slurp不能与--jq组合而本地exit1；去掉不兼容参数后正常逐页只读检查301条评论无匹配，不是网络重复POST或正式worker重试。原始错误及修正保留在工具记录。

## 完成与未完成边界

independent_contract_accepted=true；candidate_unchanged=true；complete_anchor_trajectories_exact=4。
qualification_claimed=false；deployment_claimed=false；confirmation_result_unchanged=true。
这次完成了接续点尚未执行的全部E-TFD1诊断，并取得案例内、受校准约束的反事实证据：仅保护首次接管不足，而仅在第一次延迟后改成等待也不足。冻结候选没有因此被修复或升级。
后继应针对整个重规划过程中旧动作前缀与新动作计划的一致性，以及旧动作执行与新行跳过的各自影响另立诊断。不得立即用身份识别、事后阈值或长期串行回退把本案例成功包装成一般异步方案。新候选必须另版本，在未用数据上确认；不拼旧数据、不追加本合同、不修改E-GFC1的判定。
