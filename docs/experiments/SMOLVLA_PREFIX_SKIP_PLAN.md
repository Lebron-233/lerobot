# E-PSI1：拆分首次接管的旧前缀执行与新前缀省略

接续ad35950f的E-TFD1，冻结候选a5e9fdde/8112f8d7及E-GFC1确认阴性不变。仅新增诊断文件，不改原src、模型、Graph、共享owner或生产队列。不是重跑旧确认去寻找成功，也不是新部署候选。

## 问题与固定干预

TFD1的7/19只在request2延迟两步、后续全部等待时仍失败，故可以在单个预定干预点拆开两个操作。因素D=request2安装前实际执行的旧动作数0/2；S=request2安装时跳过的新动作行数0/2。所有后续请求一律等待且S=0。
已见身份task7/state19与task7/state18。每身份四格一次，8个episode：state19按D0S0、D2S2、D0S2、D2S0；state18按D2S2、D0S0、D2S0、D0S2。原Env seed1160700+state、policy seed1170700+state不变。后续模型均用当前真实反馈；不使用存档命令驱动。

请求观测位置固定为物理action_index=0、20、40等，避免S不同导致队列长度阈值在38/42提前或延后触发下次请求的额外混淆。对角00/22按原TFD的00/20轨迹应恰好重现相同请求位置；非对角只能作为刻意改变时间语义的诊断。原threshold30/P90等生产参数不改，但不声称诊断的固定网格是生产原控制器。
所有输出保持原始50x32、归一化50x7和后处理50x7，不平移、填充、伪造模型输出。独立诊断行游标选真实输出行，记录实际action_index、source_row、nominal_action_index=observation_index+source_row及两者差值。D!=S期间非零偏移显式披露，不称作正确的观测原点接管或部署修复。
D=2时执行两条真实旧动作，未到剂量不安装；到剂量模型未完成则等待。D=0先等结果。因人为等待和反事实行号，运行时间不是实时收益。20Hz、50行、十步解码、原工厂/256双相机/relative OSC/10settling/280measurement保持，未更新权重/RTC/真机。

## 解释前的校准

四个对角完整轨迹必须逐值重现TFD1：19的D0S0对episode0，D2S2对episode3（仅首次两步）；18的D0S0对episode5，D2S2对episode6。逐个命令及来源行、全部物理状态/图像、请求输入/noise/完整和后处理输出、动作数/成败exact，不只校验结局。
8格前21帧及request1/2全部输入与输出必须等于同身份D0S0原存档。每次消费剂量与跳过行数、固定20网格、非对角行号偏移逐项独立审计。校准不成立不得作机制解释，不放宽容差或重跑。
所有四格不论方向报告。D0S2与D0S0隔离没有先执行旧动作时省略新前缀的干预；D2S0与D0S0隔离保留新前缀时先执行旧动作的干预。两者仍改变后续物理轨迹，不能从终局确定唯一接触机制。每格仅一次、已选失败/邻近成功案例，不给总体成功率或部署阈值。

## 预算与流程

新增8Env/80settling/最多2240measurement；最多160请求/episode、1280总请求；VLA1、每请求真实双相机编码及Graph replay一次；8capture，内部setup8/warmup24/capture8共40次额外sampler计账。捕获只在首次bootstrap，同owner关闭/恢复/释放后join、再关Env。
预检合成CPU测试和Ruff、冻结新增源码并普通推送、一次CPU prepare绑定原候选/权重/源文件/环境/历史与21份旧pending、单次预登记查重/POST/真实ID GET正文exact后运行。唯一目录、attempt1/retry0，不替换/补样。正常任务失败不停止预定8格；技术首错、来源变动、非有限、剂量错误/未知调用封存。
模型90s/request15s/native30s/episode100s；每episode1200个20Hz时隙，soft1500/hard1530/工具1620。监督只管理自有进程，TERM后5秒再KILL，返回后一次独立CPU审计10阶段及所有调用。审计不重新执行模型/编码/后处理/物理仿真，原实验成本保留。
必要新登记/结果发布沿用Issue#1已获授权；明确安全权限拒绝不换工具绕过，未知状态先读取。此前TFD结果评论POST被拒是历史，不以补发该评论作为本新诊断前提。普通网络故障可核实后最小修正。不清理旧文件、不强推、不进入真机；E-GFC1原分母/置信界/阴性与资格false均保持。
