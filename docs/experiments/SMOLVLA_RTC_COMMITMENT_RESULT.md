# E-RCC1：RTC 执行承诺一致化的四臂开发结果

日期：2026-09-22。执行HEAD：586dab6672270efab35ccbe36a9aeccafdf9e3dc。
预登记实际ID：5770313380；单次POST、独立GET原始JSON及正文exact后启动。
原始输出：outputs/smolvla_rtc_commitment_586dab66。

## 结论

本轮实际新执行40个episode，并完成一次独立CPU审计。串行9/10、无RTC对齐异步9/10、原动态RTC7/10、新承诺RTC7/10。新旧RTC在全部10个身份的二值成败完全相同，均在0/10、2/11、7/18失败，均恢复7/19。

新候选81次正常接管全部满足“提交时声明C = RTC条件步数 = 实际消费量 = 实际trim行数”。这些结果均在承诺边界前准备就绪，零迟到计算等待、零空时隙、零underflow/expired。两个任务保持门仍未通过，development_followup_supported=false。候选不替换基线，不进入独立确认。

本轮支持的具体判断：仅把原预计覆盖值兑现为真实执行承诺，不足以恢复此固定RTC方案的任务保持。不能继续把预计与实际前缀不一致当作唯一解释，也不能据此断言所有承诺或所有RTC方法无效。完整RTC中C之后到horizon10的软一致性约束并未消除；其影响是待检验问题，不是本轮已证明根因。

## 1. 改动范围与唯一候选

新增CommittedPrefixOwner，仅改变新候选的结果接收时机。提交时C沿用原估计：初始7，随后ceil(20×最近50次完整请求P90)+1，cap8。C照常送入模型作为inference_delay。

消费量<C：不安装早到结果，继续执行原旧动作；达到C：使用原receive，未完成则阻塞等待，不额外消费旧动作。原RTCExecutionQueue仍只按实际已消费量trim一次。任务提前结束时取消剩余承诺，关闭队列后收回请求，不为补足承诺继续移动。

原control_loop、Graph执行器、RTCPrefixOwner的服务路径、DynamicRTCPredictor、RTCProcessor、模型权重全部字节不变；不修改起始行，不变为row0保序，无新训练。EXP/horizon10/guidance10、完整Jacobian/VJP、7D掩码、50行十步、20Hz、threshold30/guard2/margin1保持。

四臂同动态runtime和双图bootstrap：serialized、aligned_async、rtc_async、committed_rtc。spec.arm选择计算类型，spec.variant单独标识四臂，两个RTC结果未合并。一个新安装合同，无长度或引导强度搜索。

## 2. 全部10组、40个开发episode

固定原8个先导身份加7/18、7/19已知诊断身份；每身份四臂各一次，按pair%4轮转臂顺序。环境和策略种子映射不变，全部实际新观测和新模型动作控制环境。不是存档命令重放；不是独立资格数据。无补样、替换、seed搜索或正式重试。

|任务/初态|串行|无RTC对齐异步|原动态RTC|承诺RTC|
|---|---|---|---|---|
|0/10|成功/143|成功/143|失败/280|失败/280|
|2/10|失败/280|成功/123|成功/116|成功/116|
|6/10|成功/146|成功/142|成功/148|成功/143|
|7/10|成功/124|成功/126|成功/124|成功/124|
|7/11|成功/116|成功/117|成功/120|成功/117|
|6/11|成功/140|成功/151|成功/146|成功/146|
|2/11|成功/119|成功/119|失败/280|失败/280|
|0/11|成功/121|成功/137|成功/134|成功/133|
|7/18|成功/141|成功/141|失败/280|失败/280|
|7/19|成功/190|失败/280|成功/169|成功/170|

表内为终局和实际动作数。失败均达到280步任务时限，非技术错误。
前8身份：7/8、8/8、6/8、6/8；后2身份：2/2、1/2、1/2、1/2。
新候选相对串行和无RTC各丢失0/10、2/11、7/18；相对原RTC无丢失，也无新增成功。新旧RTC的10/10成败相同不等于轨迹相同，更不证明总体等效。
本轮原RTC7/10不同于E-RDF1的8/10：两轮实际并发时序不同，并未冻结跨轮完整轨迹。不能把跨轮差异作为单独因果估计；本轮以同期四臂为准。

## 3. 全部在线指标

|指标|串行|无RTC对齐异步|原动态RTC|承诺RTC|
|---|---:|---:|---:|---:|
|成功|9/10|9/10|7/10|7/10|
|测量动作|1520|1479|1797|1789|
|control wall/s|79.336892|73.669841|89.572649|89.173684|
|空时隙|73|0|0|0|
|underflow/expired|0/0|0/0|0/0|0/0|
|请求含bootstrap|81|79|93|93|
|非bootstrap请求|71|69|83|83|
|实际guided请求|0|0|83|83|
|平均完整请求/ms|79.274|85.738|131.482|131.370|
|P95/ms|87.399|97.185|149.944|148.132|
|P99及最大值/ms|91.897|104.590|165.872|174.765|
|ceil(P99秒×20)+1|3|4|5|5|
|超350ms请求|0|0|0|0|
|提交至完成最大/ms|94.204|106.579|168.613|176.795|
|模型/native区间交集数|0|139|253|253|
|结束完成但未安装|0|2|0|2|

306个非首次完整请求全部低于350ms，含4个停止取消。nearest-rank P99在这些分母下为最大值，不是长期尾延迟或硬实时保证。完整请求由原owner计时，含前缀搬运、预处理、新noise、真实双相机编码、完整采样及引导、后处理、同步和CPU复制；提交快照与排队另列，磁盘写入在外。

承诺RTC控制wall相对串行增加12.3988%，相对无RTC增加21.0450%，相对原RTC降低0.4454%。不同动作数、轨迹和成败混合在其中，不能称等质量纯加速，也不对很小差异作显著性推断。零空时隙并未带来更好的任务结果。

## 4. 承诺确实实现，不是配置开关

81次正常安装的[C,actual,prefix_length]分布：
- [7,7,30]：10次；
- [4,4,30]：54次；
- [5,5,30]：17次。

独立审计逐请求核验历史估计、声明、真实模型输入、实际消费、边界、前缀来源、权重和一次trim，全部一致。候选83次guided请求包括2个结束取消，不把取消从时延分母移除。

81次结果均早于或等于承诺边界完成。早到结果累计保持7.723053秒，平均95.346ms；保持期间继续执行已声明旧动作，不是机器人累计空等7.72秒。边界之后的剩余计算等待为0；实际receive总耗时0.064358秒另记，不能称全部安装无任何CPU成本。

迟到等待、耗尽和提前结束机制通过合成测试；正式数据未出现迟到，故不声称真实GPU压力下迟到行为已验证。控制器声明承诺会延后早到结果安装，并可能延长对旧计划的依赖；不能把本合同当作免费且必然有益的改动。

## 5. 保存轨迹的进一步核查（事后描述，未新增模型/环境）

十个身份的原RTC与承诺RTC，在第一次重规划均具有相同的前21帧物理观测、8项非前缀模型输入、噪声、归一化旧前缀、RTC权重、完整50×32输出及后处理输出，逐值相同。两臂初次生成所用C均为7。
原RTC首次实际消费为3步（7例）或4步（3例），新候选均实际执行7步再安装。所有10个身份的最终成败仍相同，虽然动作数和后续反馈可以不同。

这提供了初次生成条件相同、安装时序确实改变的证据，不是模型换权重或Graph数值差异的实验。后续在线估计、时序和反馈变化，不能据此把终局不变解释为所有时序因素无作用。本轮否定的是这个固定候选的充分性。

## 6. 实际执行与审计

本轮新增VLA加载1，346次正式完整请求/真实双相机编码/Graph重放。40Env、400settling、6585measurement；底层6985native_step包含settling，不重复把外层6585environment_step算成额外动作。
80个Graph捕获，setup80/warmup240/capture80共400额外sampler调用独立计账。每episode两图在control开始前完成，控制中未重捕获。四臂图准备合计189.774756秒；bootstrap均值4.879/4.849/4.782/4.823秒，全局最大5.776931秒，不能用131ms后续请求代替冷启动。

独立审计：40episode、10组四臂首次完整输入/噪声/输出与初态exact、14196调用闭合、42/42阶段、40同owner恢复/释放与Env关闭；80图全部计账。无首错、强制终止、pending/active。worker1658406及外层exit0，UTC02:22:49.679207至02:33:03.676937，监督613.997750秒，/proc已核实进程不存在。attempt1/retry0。

153项CPU回归通过、1个既有CUDA测试跳过，20.43秒；Ruff、暂存差异和原生工厂预检通过。未改模型/Graph代码，因此未重复已完成的动态482请求资格测试。737份来源和源码、原21份pending保持。
CPU独立审计没有重跑模型、视觉编码、VJP、后处理、物理仿真或GPU计时。它验证保存输入/来源/数值/行为时序/统计和退出；不是346个新输入各自额外重跑eager。首次相同输出不推导后续各臂输入或输出应相同。

## 7. 判定与后继路线

independent_contract_accepted=true；frozen_baseline_unchanged=true。
8项开发门中6项通过，两项对串行/无RTC的任务保持失败；development_followup_supported=false；new_qualification_claimed=false；deployment_qualified=false。

原Graph异步工程实现继续保留，原E-GFC1独立确认失败结论不改；新承诺RTC不替换基线，也不追加确认样本。原独立确认32对不与本轮已见10身份合并。

下一步不继续盲调delay或接管起始行。优先明确区分：已经承诺执行的前缀一致性，与尚未承诺动作的反应能力。先检验仅对已承诺范围施加显式目标误差、取消其后的软尾目标是否更合适，并保留同承诺无RTC/原EXP作为对照。这是新的算法假设，尚未实现、预登记或运行，不能声称任务已经恢复。详见SMOLVLA_RTC_COMMITMENT_NEXT.md。

官方资料核查（仅语义参考，不用外部结果代替本地证据）：https://huggingface.co/docs/lerobot/rtc 描述execution_horizon的平滑/反应权衡，并提供ZEROS前缀二值权重；https://www.pi.website/research/real_time_chunking 强调已承诺动作一致性与对新观测反应能力须兼顾。本轮没有证明软尾约束是唯一根因。

## 8. 来源及工具状态

运行前代码已正常提交/推送，实际登记5770313380在唯一GET中保存原始响应JSON并核对正文。结果归档和发布状态以输出publication_receipt.json为准；本报告撰写时尚未执行结果发布。
本轮GitHub连接器增量评论读取返回FORBIDDEN，未通过其他工具重做该被拒读取；本实验新登记和发布是独立必要操作。正式运行、审计及代码写入未发生新的安全拦截。保留原dirty文件，不reset/clean/stash或更改生产默认。无后台遗留实验。
