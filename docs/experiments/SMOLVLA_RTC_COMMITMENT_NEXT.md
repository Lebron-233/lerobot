# E-RCC1之后：只研究承诺前缀，停止把旧计划尾段一致性当作必然收益

状态：586dab66固定40episode结束，独立审计通过；serialized9/10、aligned9/10、RTC7/10、committedRTC7/10。81次正常承诺接管C=实际消费=source_row，无迟到等待，却仍丢失0/10、2/11、7/18。旧结果与基线冻结，不继续本合同、不重跑到阳性。

## 路线判断

继续保留Graph、真实反馈、原观测原点和一次实际消费trim作为已验证工程底座；不让未通过任务保持的RTC变体成为生产默认。无条件row0、完整EXP引导及仅绑定覆盖量的候选都未提供足够的任务保持证据。不能再把相同输入输出、低延迟或前缀MSE作为任务成功的替代指标。

下一条最小候选：生成只在已声明的C步上施加旧动作目标，未承诺尾段不再直接要求贴合旧计划。明确使用w[j]=1(j<C)，否则0的prefix-only权重，对应官方RTCAttentionSchedule.ZEROS的语义；不是将所有引导关闭，也不是硬夹紧前缀。完整Jacobian仍能通过动作维度之间的耦合影响尾段，不能声称后缀数值完全不变或前缀精确满足。

这是算法权重范围改变，非原EXP等价优化。不得静默修改现有DynamicRTCGraphRuntime的EXP校验来绕过合同；须新增实验版本和显式配置检查，不修改已有SRC或输出。不得同时扫horizon/strength/C后选最好者。

## 下一份有界实验建议，尚未实现/登记/运行

A. 固定正确性/成本检验：复用E-RDC1的16个原开发输入、225种可提交d/L组合及16个无前缀校准；新的prefix-only原生eager与新Graph共482请求、241对完整/归一化/后处理逐值相同。参考对象是同一新权重公式的eager，不要求和旧EXP输出相等。C由提交前信息确定，所有模型权重、十步/50行/20Hz、7D有效坐标和完整VJP不变；图数量、缓存刷新、准备/重放/CPU复制及冷启动单独计账。技术与全部225次Graph<=350ms通过后才允许进入B。

B. 开发闭环拟固定5臂×10个已见身份=50episode：原serialized、原aligned_async、同承诺规则但无RTC、同承诺规则的原EXP-RTC、新prefix-only承诺RTC。这样可区分单纯强制继续旧动作与生成期约束。保留全部10个身份与固定种子映射，不只运行0/10或7/18。新候选必须保住所有同期对照的成功身份、满足350ms/cap8、真实重叠/等待及退出检查，否则不晋级。

所有承诺臂先沿用同一原覆盖估计规则；不同计算路径仍可能导致每次C不同，完整记录并报告。这是同调度规则的候选对照，不冒称每步C相同的纯mask因果估计。若进一步要隔离每步承诺量，必须另立共同固定承诺日程的诊断，不从本批终局事后挑日程。

B的代码、顺序、单episode和总体预算、晚到策略、首错停止、一次正式attempt及审计必须在执行前完整冻结并独立登记。本文件仅规划，不能当作已启动或已发布的预登记。前阶段失败不自动运行后阶段，不扩大当前40episode。

## 成功、失败与证据边界

该路线有可检验动机，但当前并无prefix-only成功数据。若新候选仍失败，优先报告训练策略对异步前缀的适配不足或需要任务阶段反馈等待检验问题，而不把所有问题强行归于延迟。新的学习方案需有训练数据和独立评价，不能直接切换需要专门训练的RTC模式。

开发门通过后再冻结新候选，用查重后的未用身份与事先规定的统计方案确认；原E-GFC1的32对、已见10身份不得混入新确认。无真机、无长期无人看管执行、无生产默认更改。旧21份pending与全部不利轨迹保留。

资料：Hugging Face RTC文档 https://huggingface.co/docs/lerobot/rtc ；Physical Intelligence RTC说明 https://www.pi.website/research/real_time_chunking 。资料支持前缀/尾段约束的概念区别，不证明当前机器人与SmolVLA检查点必然获益。
