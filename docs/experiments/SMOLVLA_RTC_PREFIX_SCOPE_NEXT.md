# E-RPI1 / E-RPF1之后：保持工程底座，不再把全局mask切换当作完成路径

状态：输入482请求与225组合等价、闭环50episode已完成并审计通过。主候选前缀限定7/10，EXP7/10，成功身份在7/18和7/19互换；串行和原对齐异步9/10。原始输出不得重跑或覆盖，旧基线和确认阴性保持。

## 已经回答与尚未回答

新旧显式目标范围确实改变了动作内容；首次重规划输入、C和实际裁剪相同的对照已保存。取消全部未承诺尾目标不是普遍修复；无RTC承诺对照也会回退，因此不能将所有问题简化为引导公式错误。当前结果没有证明任何方法族不可能，也没有证明训练期不兼容是唯一根因。

## 建议收敛路线（本轮没有启动）

保留Graph/真实反馈/原观测时间对齐与一次trim作为固定工程基线。停止继续在相同十身份上扫全局schedule、horizon、C或strength，不将7/18与7/19的终局标签用于人工选EXP/ZEROS。任何在线选择必须只用提交时可见信息，并有独立训练与未见验证，当前没有这样的证据。

优先把下一阶段定义为“前缀条件化策略的可行性核查”，而不是直接追加同类推理期启发式。检查可用且来源明确的机器人演示是否包含足够的当前观测、已经执行的动作前缀及后续动作；先按轨迹划分训练/开发/封存验证，避免用当前十个结果调参再称独立。仅用旧策略输出自我蒸馏，不能自动提供修正旧失败的监督。

在训练前先确定普通同预算微调对照和前缀条件微调候选，明确可训练模块、损失只在未执行后缀计算的语义、C分布、训练步数和单次预算。先在微型合成数据检验前缀/后缀索引、逐动作时间嵌入和梯度，再决定是否值得真实训练；缺少可靠后续动作数据则记录数据缺口，不通过扩大神经模块来补证据。

这是新模型候选，需要新的源码和检查点版本，不假装为当前SmolVLA的配置开关。官方main文档的trained RTC目前明确用于兼容且经过前缀训练的Pi05检查点；不能直接将当前SmolVLA配置改为trained或更换模型后继续声称完成原模型对照。文档仅支持训练条件化路线存在，不保证本SmolVLA任务获益。

数据与接口可行后，另立完整训练/冻结/评价合同，保持同Graph正确性、原控制频率与时延预算。必须同时比较普通微调和前缀条件训练的任务保持、并发实时性及所有回退；成功后才用查重的新身份独立确认，旧32对与本十身份不能重复标成未见。重训练后的Graph输出须相对该新检查点的eager重新验证，不继承旧权重的等价结论。

在此期间，原无RTC Graph只能作为已验证工程原型而非生产默认：其先导和确认数据仍存在回退。实时资格、任务总体非劣和真机安全不能由工程时延通过推导。无自动真机、无限运行或未经数据审查的训练。

## 接续与来源

本轮的两阶段正式worker与独立审计都已退出。先读SMOLVLA_RTC_PREFIX_SCOPE_RESULT.md及INPUTS/FEEDBACK_AUDIT.json，核验publication_receipt.json；不要重复prepare/POST/模型运行。21份旧pending继续保留；明确权限安全拒绝停止相应动作，普通网络错误核实后处理。

官方资料（2026-09-22查阅）：https://huggingface.co/docs/lerobot/main/rtc 与 https://huggingface.co/docs/lerobot/main/en/pi05 。ZEROS是二值显式前缀目标；trained RTC需兼容的专门训练检查点。该后继方案尚未实现、登记或执行，不构成成功承诺。
