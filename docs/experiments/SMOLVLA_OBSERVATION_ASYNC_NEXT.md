# E-OBS1之后：已完成闭环pilot，但不跳过在线尾延迟与任务保持

2026-09-17。执行代码7887dfe7，16episode/8对，独立动作来源与时序审计通过。这里是后继计划，尚未派发。

## 当前不能省略的两道关

1. 工程链成立不等于任务保持：串行7/8成功、异步6/8；两对仅串行成功，一对仅异步成功，不能只报告空时隙298降0或wall合计减少12.99%。小样本不作统计结论。
2. 离线合格不能直接继承到闭环：异步完整请求P99=375.470ms，既有20Hz/cap8/一行余量需要9步。实际最大delay8且无过期，不等于余量预算通过。

## 优先路线：保持算法的无RTC执行优化

保持原权重、观测原点、按实际消费一次trim、50行动作块、十步解码、20Hz、cap8/余量1，且无学习预测器或RTC引导。
研究把既有SmolVLAGraphRuntime接入本次共享owner/controller，不能恢复旧未来起点row0语义。所有Graph捕获/预热成本显式登记，不得把它们隐藏成零。
先核对当前Graph接口与生命周期：单owner、episode独立缓存、固定噪声/语言/状态、所有动态输入更新正确、关闭时恢复sampler。以原16开发输入做完整50x32输出exact对照，不读取封存资格集。
模型和动作语义不变的前提下，单独注册完整请求/并发开销测量；P99仍按同350ms上界，保留全部慢请求。不能只用decoder时间或用Graph对eager的跨轮背景变化冒充严格收益。
这是候选实施方向，不承诺Graph一定达到预算；若不能保持输出等价，先定位接口差异，而不是放宽exact或直接进新Env。

## 随后的任务保持问题

只有技术等价和并发预算成立，才定义新的配对闭环。完整报告相同初态的任务成败、动作数、wall、队列等待、接管跳变及所有不利例。
若需强非劣主张，要在新确认数据揭示前固定样本量、主要终点、置信区间和停止规则。E-OBS1八对是已使用pilot，不能调参后重标独立确认。
可先在已保存开发轨迹上描述旧执行前缀与新块前缀的差异，作为动作衔接诊断；它只是诊断，不能把差异归因为唯一失败原因或以对旧动作更接近代替任务成功。
当前guided RTC完整路径超预算，不能直接加回以掩盖衔接问题；保持完整Jacobian/合法维度掩码，不以回退错误公式换速度。

## 执行边界

本轮E-OBS1结束，attempt1/retry0，所有worker已退出；不重新启动旧目录。上述后继需独立代码、测试、登记和预算。
不得降频、扩大cap、过滤慢请求或替换失败初态使原条件通过。旧未来视觉预测阴性和IQ1/ACQ资格封存不变。
明确权限/安全拒绝不换工具绕过；普通接口问题可以透明最小修复。保留旧21份pending；正常显式提交/非强制推送，禁止reset/clean/stash/force。
