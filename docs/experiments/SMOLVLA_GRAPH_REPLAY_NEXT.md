# E-GCR1之后：将已核验Graph接入真实观测原点闭环

2026-09-17，依据E-GCR1的206个exact输出、2820条相同原生命令重放与独立CPU审计。本文待执行，不是新闭环结果。

## 不再重复的工作

固定完整输出与原生并发负载时延门已通过：Graph并发P99=122.374ms，小于350ms，原算法/权重/十步保持。无需重跑本轮挑选更小延迟，也不重训预测视觉模型。保留所有旧阴性与资格封存。

## 最小接线

在原E-OBS共享InferenceOwner/control_loop/RTCExecutionQueue上增加可显式注入的Graph predictor及owner资源关闭钩子，默认旧行为不变。优先复用FullPath与既有SmolVLAGraphRuntime，不复制控制器或用运行时修改模块全局变量切换实验。
真实新请求从当前Env观测组batch，以原专用CUDA Generator产生noise；禁止使用本轮来源图像、来源noise或来源命令充当新闭环输入/控制。归一化7D、模型50x32、环境7D分别保留。
Graph只能在其唯一model owner中建立/重放/关闭；单episode首次bootstrap完成捕获后才ready。控制内形状变化/额外捕获首错停止，不能隐式重捕获。退出先关闭队列，收回最后请求，同owner恢复sampler/释放Graph，join后关闭Env。首次平均约1.375秒的代价必须报告，不能藏入稳态。
保留观测索引原点，实际消费多少行只trim一次，安装只在Env返回边界，阈值30/P90窗口50/初始delay7/cap8/余量1/guard2、50/10/20Hz均保持。更快返回会改变执行时序，这正是新闭环需要检验的内容。
独立审计应明确区分Graph replay元数据与eager projection hook，不能伪造每次Graph重放都触发10个Python投影钩子。加入同一输入异步资源生命周期、关闭中请求、shape变化和禁止控制内capture的CPU合成回归。

## 下一个有界试验

拟同Graph两臂serialized与async，任务0/2/6/7各2个新初态，共16episode。拟state10/11尚未查重或冻结；准确身份必须先查整个仓库元数据，注册后不得替换。旧8/9只能做诊断，不能并入新确认分母。
每对初态checkpoint及bootstrap完整输出exact，次序轮转、固定种子和一次尝试，不补正常失败。全流程使用真实反馈；所有成功/超时/技术失败完整报告。
主要技术终点：全部实际动作来源、观测与chunk行号、一次trim、单owner/单在途、关闭及native账本。并发线上完整P99仍需ceil(P99*20)+1<=8并报告最大实际delay；不得继承本轮受控重放的122ms作为实时控制器已通过。
性能与任务结果分开：控制wall、等待/underflow/expired、重规划接管、切换动作跳变、请求排队与完整时延，以及每对成败/动作数。不得把较短wall但失败算作改善。
小样本只能描述。没有更多串行成功被异步丢失也不等于统计非劣；任何强任务保持/部署主张必须在新数据揭示前另定样本量、可接受差异和置信区间。

## 停止与出版

先完成接线回归、新代码冻结与单独预登记，再派发真实闭环。本轮E-GCR1到已审计时延成果为止，没有自动派发这些Env。保持不绕过权限安全拒绝、不清理用户文件、不强推、无后台遗留。
