# E-GFB1：Graph已接入真实环境反馈，异步8/8成功且在线尾延迟满足预算

2026-09-17。execution HEAD `8112f8d7e672a2462639c7f612a0c03e444c7e21`。
预登记实际评论5713573753，运行前单次POST、实际ID独立GET与body exact已保存。
原始目录：`outputs/smolvla_graph_feedback_8112f8d7`。

## 结论

Graph执行器已接入真正使用环境反馈的观测原点共享控制器，16/16个episode、8/8配对初态和首次完整输出/noise一致，独立CPU审计通过。
异步8/8成功，串行7/8；没有串行成功而异步失败的配对。异步完整非首次请求P99=97.020ms，两臂均满足原350ms预算；异步无动作时隙0，实际模型/native重叠96个区间交集。16个runtime均在各自owner线程内释放和恢复sampler后join，再关闭Env。
这完成了当前固定硬件/权重/4任务/8新初态条件下的Graph异步反馈工程pilot，不是统计非劣、普遍100%成功、硬实时保证、真机或部署资格；realtime_qualified=false保留。guided RTC原时延失败和学习未来视觉的旧阴性不改写。

## 与前轮的关键区别

本轮每个请求来自当前Env返回的双相机/状态，独立CUDA Generator生成新noise，新动作经后处理和队列实际控制Env。没有使用E-GCR1旧图像、旧noise或旧命令代替新反馈。
两臂都采用同Graph、原SmolVLA冻结权重、50行动作块、十步解码、20Hz、cap8/余量1/guard2/阈值30/P90窗口50/初始delay7。
control_loop、estimate_delay、should_submit的语法树保持不变。唯一调度差别仍是串行等待、异步继续旧动作；按实际已消费行数只trim一次，提交与安装只在Env返回边界。
Graph首次捕获仅在bootstrap，控制内丢失runtime/输入签名变化均会失败，不允许静默重新捕获。所有创建、重放和释放在同owner；旧eager接口默认行为不变。

## 全部配对结果

S表示成功，T表示280步正常TimeLimit；没有替换、删除或追加样本。

| task/state | 串行结果/动作数 | 异步结果/动作数 | 串行控制秒 | 异步控制秒 | 异步−串行秒 |
|---|---:|---:|---:|---:|---:|
|0/10|S / 143|S / 143|7.472862|7.118013|-0.354850|
|2/10|T / 280|S / 117|14.622670|5.826449|-8.796221|
|6/10|S / 146|S / 142|7.617198|7.066690|-0.550508|
|7/10|S / 124|S / 126|6.470684|6.274113|-0.196572|
|7/11|S / 116|S / 117|6.015739|5.822656|-0.193083|
|6/11|S / 140|S / 149|7.271227|7.423211|+0.151984|
|2/11|S / 119|S / 119|6.173981|5.922999|-0.250982|
|0/11|S / 121|S / 137|6.344279|6.820634|+0.476356|

七对都成功，一对仅异步成功，没有仅串行成功或两臂均失败。成功数8/8对7/8只描述本pilot；一个不一致配对不足以证明总体成功率提升。

| 全8条/臂 | 串行Graph | 异步Graph |
|---|---:|---:|
|成功|7/8|8/8|
|实际动作|1189|1050|
|控制wall合计/秒|61.988641|52.274765|
|无动作时隙|55|0|
|underflow / expired|0 / 0|0 / 0|
|请求（含bootstrap）|63|56|
|实际后续接管|55|47|
|停止时完成但禁止发布|0|1|
|模型/native区间交集|0|96|
|最大实际消费delay|0|2|

总wall减少9.713875秒、15.6704%，6/8配对较短；但轨迹、动作数和任务结果不同，不能解释为同轨迹任务加速。
55个空时隙少2.75秒，139个较少动作少6.95秒，共约9.70秒，与wall账目相符。任务2/10贡献8.796221秒差异；不将这全部归为隐藏等待。
事后仅描述“两臂均成功”的七对，控制合计47.365971到46.448316秒、下降1.9374%；这个选择后的子集不是主结果，也不是独立验证，主分母始终全部8对。

## 实际反馈闭环的完整请求时延

以下是owner接收当前观测并调用完整预测器的非bootstrap时延，含预处理、fresh noise、输入指纹、实际双相机编码、prefill/十步Graph解码、后处理、数据同步及CPU证据复制。文件写入在外，owner排队另报。没有沿用重放时延作为本轮数据。

| 指标 | 串行Graph | 异步Graph |
|---|---:|---:|
|非bootstrap请求数|55|48|
|平均/ms|72.950|83.537|
|P50/ms|71.473|84.212|
|P95/ms|81.192|88.849|
|P99及最大值/ms|82.112|97.020|
|ceil(P99秒×20)+1|3|3|
|owner排队均值/ms|0.376|0.371|

两臂均满足cap8/余量1条件（350ms）。异步实际delay最多2步，没有耗尽队列或超期安装。
48/55观测的nearest-rank P99恰好为最大值，不是对总体尾部精确估计；离线到在线的本轮预算接纳也不是硬实时保证。
停止时的task6/state10/async request8完成耗时78.365ms，因episode已结束而拒绝发布；它仍在48次时延和56次总请求分母内，不是错误丢块或隐藏调用。

## 冷启动与动作衔接限制

首次bootstrap含捕获：串行平均1.298086秒、最大1.796373秒；异步平均1.217606秒、最大1.232731秒。该差异不作为异步冷启动优势，因为首轮全局状态与顺序不同。
16次捕获准备合计18.739565秒，单次最大1.473486秒。内部setup16/warmup48/capture16共80次sampler额外调用独立计账；首次准备在control t0前完成，不能把约84ms稳态时延当作冷启动。
切换后处理7D命令L2：串行55处均值0.527046/最大2.473768；异步47处均值0.342594/最大2.133333。轨迹和切换次数不同、坐标尺度混合，这只是预定描述，不构成同轨迹平滑度提升、安全阈值或任务成败的唯一解释。

## 调用、审计与退出

- VLA加载1；正式请求119，每次真实双相机编码及一次Graph replay；新Env16、settling160、measurement2239。
- 16次bootstrap Graph capture，80次内部sampler额外调用另计。没有训练、RTC引导、学习预测器、旧资格标签读取或真机操作。
- 全部2239条实际动作的来源、索引、一次trim和实际native命令接纳；119份输出与记录核验。这里只对8对bootstrap要求配对完整输出/noise exact，不声称新119份输出与某套未运行eager模型全数对照。
- 独立CPU审计4869个intent/return、18/18phase、16次同owner释放及恢复sampler通过；无未知调用或未结束phase。
- 独立审计不重新编码图像、执行模型/后处理或物理仿真。当前输入指纹绑定保存的实际worker输入，不能替代从RGB重新计算token。
- worker738797、外层和审计exit0，/proc/738797已不存在；监督UTC2026-09-17T11:27:31.645622至11:30:25.869935，wall174.224355秒，首错/stop_reason为空，无强制终止，attempt1/retry0。
- 702份来源与源码哈希保持，21份原pending字节/状态保持。52项CPU回归、Ruff通过；一次import格式错误在冻结前修复，不是正式重试。本轮没有安全权限拦截。

独立字段：graph_feedback_chain_completed=true、online_latency_budget_passed=true、asynchronous_overlap_observed=true、waiting_reduction_observed=true、no_serialized_success_lost_in_pilot=true。
生产/强统计字段：realtime_qualified=false、statistical_noninferiority_claimed=false、risk_thresholds=null。

## 当前完成边界

本轮完成“Graph执行器＋真实反馈＋异步队列”的有界原生仿真工程验证，具备支持后续独立确认的候选实现。不能称原guided RTC三臂实验通过，也不声称解决预测视觉补偿泛化。
后续应固定部署候选并开展未用身份的任务保持与尾延迟确认，而不是继续改参数后复用本轮8对作为独立验证。任何强非劣/泛化/生产验收需提前定义可接受差异、样本量与置信区间。本合同结束，没有启动新确认或遗留后台实验。
