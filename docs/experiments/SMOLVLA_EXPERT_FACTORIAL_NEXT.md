# F-EAT1接续：先检查有效学习幅度，不把小更新阴性当容量结论

状态：F-EAF1的6次技术更新和审计通过；F-EAT1四臂各128更新、320十步解码、独立审计完成。D专家前缀相对B投影前缀的suffix仅-0.007653%、first+0.034541%；交互收益suffix+0.000059576637、first-0.000170884558，九门仅2通过。四份新检查点冻结、不晋级，不重跑原唯一输出，不续训到阳性。

## 已回答

32个实际自注意力Q/V适配Linear确实执行，梯度与更新存在，加入286720参数成本可接受。当前rank8/alpha8、lr2.5e-6、128步方案没有获得所需前缀质量增量。不能把同条件微小差别说成显著收益，不能用D相对无条件的信息收益替代学习收益。

## 尚未回答

这是否足够检验容量瓶颈？尚不够。保存的有效DeltaW聚合范数仅约base的0.01715%，不证明该可训练子空间已经被充分优化。也不证明BF16使更新无效，分支并未合并权重。单rank/seed、12train/4dev不能推断所有专家适配。原专家全参、K/O/MLP/交叉注意力并未开放。

## 后继最小问题：训练侧有效更新与函数响应诊断（未登记、未运行）

在冻结本轮数据/结果后，另立有界技术合同，优先只用train中预先选择的固定小批，不接触4dev或4sealed来挑幅度。先无新更新地检验已保存adapter开/关在相同训练输入、noise/time/C下对后缀velocity、十步动作和前缀敏感性的实际影响，记录每层DeltaW、梯度、激活增量的尺度与精度边界；不得只靠权重范数断言表示能力。

若有必要做小批可学习性检查，应预登记固定更新预算和评估时刻，分别比较仅投影与投影+专家适配能否降低训练目标并改变目标方向的动作。按样本一致的mask/坐标监督，不把不断换样本的相邻训练loss当作收敛曲线。保留普通/前缀对照；需要调整adapter参数组学习率时先说明只在train侧标定，旧2x2结论不回写、不给四条dev选择最优学习率。

只有实际函数响应、训练学习信号及资源合同成立后，才考虑另一次更充分但固定的受控微调；不能自动扫rank、训练步数或扩大网络。新实验应明确是优化强度问题还是数据覆盖问题，不能同时改数据量、可训练模块、目标权重与训练预算后单因素归因。当前数据沙拉酱仅1条训练轨迹，需报告覆盖限制。

## 仍需守住的最终目标

学习式前缀接口本身不是SmolVLA异步任务保持完成。正确专家前缀与实际旧策略承诺前缀不同；错误前缀改变到达状态，不能无条件把原专家后缀作为该反事实状态的真值。后续任务候选必须经新权重eager/Graph等价、原20Hz/50行/10步/350ms预算下的真实队列反馈与任务保持，再用未使用身份独立确认。不能继承原Graph资格或以动作MSE代替任务成功率。

原Graph控制器/观测原点/实际消费一次trim不变，继续保留原无RTC工程参照但不设生产默认。原E-GFC1和所有RTC不利结果不变。当前四条dev是已见开发，不合并旧样本或重标为未见。

## 精确冻结原件

前置：outputs/smolvla_expert_feasibility_985ee5c0；正式：outputs/smolvla_expert_factorial_62cb5f6b。
A checkpoint SHA38f47e33d2fa29399bbbb046be7ff02022e8d9100db6c46fc71f82049d5cb0dc。
B checkpoint SHAac35c5a7210cf1cfbf6fc1e87e27628cc676dbee97a35b1533737366eeb76c35。
C checkpoint SHA2fd65c841c9884aa24e2f22a4e3b899c3e28ea3902c79103f61a95f467f95974。
D checkpoint SHAd20c3ff3b5bb8841cd97184110ef59ff6acbf3f9ebd9af1f53138acd5bd7e932。

这些是base来源明确的绝对投影值、独立LoRA参数及optimizer，不是整模型，不是可将整个checkpoint直接相加的delta；deployment_ready=false。已有6次前置更新已还原，不能当主训练起点。

下次先核验结果与publication_receipt及退出状态，不重复prepare/POST/训练。必要原分支发布权限沿用，明确安全拒绝停止对应动作；普通网络未知先查真实状态，不盲重发。无真机、依赖升级、旧21pending清理或无人看管任务。
