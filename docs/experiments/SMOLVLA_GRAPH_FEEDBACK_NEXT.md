# E-GFB1之后：从有界工程原型转入独立确认

2026-09-17。E-GFB1实际反馈控制16episode完整审计通过；同Graph异步8/8、串行7/8，P99分别97.020/82.112ms，异步空时隙0，16个runtime关闭恢复成立。新实验未在本文启动。

## 已完成的候选

GraphFeedbackPredictor复用已验证FullPath/SmolVLAGraphRuntime，接入共享InferenceOwner/control_loop/RTCExecutionQueue；真实当前观测生成动作，观测原点/一次实际消费trim/Env返回边界安装保持。
原同算法50行/十步/20Hz/cap8/余量1。此候选在当前4070Ti SUPER与固定LIBERO环境的工程pilot上成立；生产资格false不是否认本轮结果，而是区分未完成的统计和部署层验收。
保留同Graph串行对照，不将E-GCR1固定负载速度比、E-OBS1旧初态任务结果与本轮新初态拼成因果性能改进。

## 下一轮必须先冻结的问题

1. 任务保持确认：继续同Graph串行/异步配对，但使用尚未用过的身份。不能把E-OBS1、IQ1/ACQ资格、E-GCR1重放或本轮10/11并入新确认。根据明确可接受的成功率损失和置信水平决定样本量；8对不足以支持强非劣。身份/种子/顺序/终点/停止规则先查重并预登记，不能测到阳性为止。
2. 在线时延稳定性：保留350ms门及首次启动成本，固定真实环境负载与请求分母，报告所有慢请求、停止取消、动作余量，不挑空闲时段或仅用模型平均时间。延长观测与不同负载需另立预算；不能把本轮P99最大值当成长期保证。
3. 使用与恢复边界：将已测试的单owner、初次capture后ready、禁止控制内capture、结果与episode身份匹配、关队列/收回请求/释放Graph/join/关Env顺序写入面向操作者的运行手册。新增故障注入仅在合成或仿真且单独固定合同，不直接进入真机。

优先保持已成立实现而不是继续搜索阈值；任何Graph缓存复用、跨episode预热或解码步数变更，都应另做语义/成本对照。当前启动捕获仍需约1.2至1.8秒，不能删除其账目。
没有必要再训练旧未来token小预测器来完成这条无RTC异步工程主线；guided RTC优化是独立增量研究，原731.916ms尾延迟失败保持。

## 停止状态

E-GFB1的119正式请求、2239真实测量动作、16episode/16capture均已结束；审计通过、无未知调用、无后台遗留。上述是后继方向，不是自动额外派发授权或已经运行的结果。
保留21份旧pending与全部旧阴性，正常提交/非强制推送，明确安全拒绝不换通道规避。
