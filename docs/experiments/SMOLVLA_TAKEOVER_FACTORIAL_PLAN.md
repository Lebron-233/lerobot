# E-TFD1：冻结候选的接管时序二因素反事实诊断

接续E-GFC1结果d3f7dae7和评论5715778740。候选a5e9fdde保持冻结，原独立确认32对的1次回退及未通过结论不改变。本合同仅新增编排、审计、测试和文档；不改已有模型、Graph、控制器或队列文件，不重新训练。

## 问题与已见数据

唯一回退7/19在request2全部模型输入和输出相同时，因串行等待与异步执行两步旧动作而开始分歧；目前未证明第一次分歧或后续累积接管分别有何作用。
选择7/19失败案例和同任务相邻7/18成功案例。两者均为已用确认身份，本轮明确转作开发诊断；不是新的独立资格，不将结果并回32对，不碰其余封存资格或旧confirmation21..40。
来源仅原E-GFC1的episode54/55/56/57。只读核实：串行全部实际delay0，异步所有已安装重规划实际delay2。新源文件hash锁定，原结果与数组不覆盖。

## 固定2×2干预：8个episode，不追加或选种子

每个身份4臂：首次重规划request2的实际旧动作消费数取0/2；后续request3及以后同样取0/2。bootstrap恒为0。
四格00、22、02、20分别回答：全部等待、全部两步旧动作、只后续两步、只有第一次两步。
顺序：7/19运行00、22、02、20；7/18运行22、00、20、02。原环境seed=1160000+100*task+state，policy seed=1170000+100*task+state保持，不搜索seed。
同原LIBERO原生工厂/资产/256双相机/relative OSC/20Hz/280动作上限/10settling。真实当前观测与新noise生成动作，不按旧存档命令驱动干预臂。

干预控制器复用冻结InferenceOwner、GraphFeedbackPredictor、RTCExecutionQueue、NativeSession、should_submit与estimate_delay。仍单owner单在途，阈值30等候选参数不变。仅固定结果安装前必须执行的旧动作数：未消费够2行就不安装，消费够后若模型仍未完成则等待；0行臂先等结果再动。新块仍按实际已消费行数只trim一次，提交/发布只在Env返回边界。
人为约束执行进度可能增加等待；这是仿真因果诊断，不是新在线候选或时延优化。不得把控制wall、成功或人为0延迟写成可部署实时性能。

## 可解释性的先决条件与输出

8臂均要求与同身份原串行在动作index0..19后的21帧状态/图像exact，bootstrap和request2的全部8项模型输入、noise、完整50×32/归一化50×7/后处理50×7输出exact。
4个00/22基线还要求整条轨迹的全部命令、来源行号、物理观测、请求输入/输出、动作数和最终成败与各自原存档exact；它们是诊断校准而非独立重试。基线不重现则不得作因果解释，完整保留不一致，不放宽容差重跑。
四格全部报告。若20单独失败则支持在后续0延迟条件下首次延迟足以改变终局；若02单独失败则支持后续延迟可在无首次延迟时改变终局；只有22失败则支持本案例中组合效应。其余可能结果据实描述，不以相邻成功例或单次实现外推总体。
即使某干预恢复成功，也不声称修好了冻结部署候选；确定固定语义下终局变化不等于证明中间接触/抓取的唯一物理机制。

## 预算、冻结、运行

唯一VLA加载1，8Env/80settling/最多2240测量动作，每episode最多160请求、总1280。每正式请求真实双相机编码和Graph replay一次。8次bootstrap捕获，内部setup8/warmup24/capture8共40次sampler额外调用独立计账，控制中不重捕获，同owner释放/恢复后join再关Env。
模型90秒、request15秒、native30秒、episode100秒。复用原监督soft1500/hard1530，工具1620秒；attempt1/retry0，8episode固定结束，正常失败不提前停。不升级依赖或驱动，不改生产默认，不操作真机。
只有CPU回归和静态检查通过、代码普通提交及非强制推送、CPU prepare绑定真实HEAD/来源/环境/旧21份pending后才能登记。唯一E-TFD1-REGISTER:<HEAD>先单次只读查重，缺失时单次独立gh POST，再真实ID独立GET/body exact存证。
监督执行唯一输出，不复用原确认目录。安全/权限拒绝停止对应动作，不换工具规避；普通网络/语法故障先核实状态再修正。技术首错/来源变动/预算/非有限/未知调用停止封存，TERM后5秒再KILL仅限自有进程。

## 独立审计与解释边界

退出后一次CPU审计，复用冻结Graph及动作来源核验，再独立验证干预剂量、4条校准轨迹、8臂共同起点、全部native命令/调用/10阶段/同owner关闭。审计不重算图像编码、模型、后处理或物理仿真，不初始化CUDA。
执行结束、资源闭合与确认原始文件hash保持必须成立。统计置信门/生产资格不更新；不添加确认样本，不用本次已知案例诊断的收益挽救旧确认。完整结果无论方向都可作为研究进展发布至Issue#1。
