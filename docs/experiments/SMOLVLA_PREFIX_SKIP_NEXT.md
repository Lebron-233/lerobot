# E-PSI1之后：从行号诊断转向可执行的前缀一致候选

状态：本轮8格完成，4条校准轨迹exact，独立审计通过。7/19的D0S0和D2S0成功，D0S2和D2S2失败；7/18四格成功。旧候选和确认阴性保持，不把反事实成功称为部署修复。

## 已有证据对下一步的约束

1. 不再只减少推理时间或只保护首次接管：实时计算已经有余量，已知案例在单次首次跳行也可失效。
2. 不直接把原trim删除：D!=S有明确名义/实际时间偏移，诊断规则不是原算法的等价优化。输入/输出数值一致不能替代跨动作块前缀兼容。
3. 不依赖task7/state19识别、手工例外或从这两例选择门限；不能让它们重新进入独立资格分母。

## 下一份新候选合同应回答的具体问题（尚未实现或运行）

优先把“保留尚未实际执行的新前缀”定义成独立保序动作计划，而不是冒用原观测时刻原点。请求所见观测的时间戳、模型计划行号、实际执行索引分开记录；明确重规划替换/拼接、旧承诺前缀与新计划重复动作的处理规则、单在途/关闭与过期边界。先做合成序列/重复执行/超时测试，不改冻结基线。
在原开发身份上固定有界对照：同Graph的原时间对齐异步、明确保序语义的异步候选及同模型串行参照。覆盖整个episode，不仅首次；仍真实并发、不人为将后续请求全部等待。固定同20Hz/50行/十步/原权重，启动成本另记，原350ms完整请求门不放宽。
必须同时核验：实际异步重叠与等待、重复/遗漏的动作计划行、观测到执行的年龄、整体任务成败与所有不利例。不能只以旧前缀MSE、138步案例成功或均值wall来宣告改善。候选不得根据未来观测或任务成功标签决定接管。
另一条可单独设计的路线是让新计划的承诺前缀与正在执行的旧动作一致（例如完整公式的RTC）。这不是本轮已完成方案，现有guided RTC完整路径超预算的旧结论不变，必须先验证正确性和全路径成本，不用省略Jacobian换速度。
选择哪条路线的正式代码、身份、次数和停止条件要在运行前冻结；不并行搜索多套方案后只报告最好者。此处不自动扩大实验预算，也不承诺某候选必然有效。

## 独立确认和停止

7/18、7/19都是已知诊断身份。新候选开发结束后再冻结，用未用身份与提前确定的任务保持统计方案确认。不能拼E-GFC1旧32对或追加本轮格挽救旧置信界。
本次worker/审计已退出，无后台实验。保留旧21份pending，旧候选锁、全部不利记录和资格false。明确安全权限拒绝停止相应动作，普通网络故障核实后修正，不用更换工具规避限制。
