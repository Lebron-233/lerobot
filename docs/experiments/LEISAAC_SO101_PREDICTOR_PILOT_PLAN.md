# M5.4-L6: new-domain future-latent predictor pilot

Date: 2026-09-07. Starting source: `5ab75cc705064df7b3e797238ead8cd48b91a1b2`.
The user's project-wide implementation/experiment authorization remains active.

## Research question and boundaries

Can a newly trained small residual predictor improve future visual tokens over
holding the current tokens, on independently seeded real SO101 simulator
trajectories generated by the task-matched fixed policy? This is an offline
forecasting pilot on actual action-dependent simulation, not a task-success or
realtime predictive-control claim. L4's task failures and L5's timing failures
remain unchanged. No old SO100 predictor weights, old test/cache/B4 samples or
old scientific approval are reused for this new candidate.

The primed L5 identity engineering run executed221 real controls with eight
planned requests and no underflow/inference-deadline miss, then failed the
unchanged control lost-slot criterion. Preserve that result; do not keep blindly
rerunning the same hardware load to obtain a pass. L6 addresses the project's
scientific predictor question in parallel with this documented runtime limitation.

## Frozen collection before any model fitting

Policy: `wsagi/SmolVLA-PickOrange@c8c3318dba152b0ba671ff07b4314418d5aa4b4a`,
same saved complete weights, native use_amp=false and native processors. Standard
front/wrist cameras with the verified nominal reset anchor, CPU PhysX/GPU RTX,
zero robot initialization, original objects/randomization/success predicate,
physics dt1/60, decimation2 and control/camera30Hz. No model or VLM fine-tuning.

Collect **seven independent reset episodes, at most600 actions each**. Environment
seeds20260920..20260926, policy seeds1900..1906 respectively. Split by episode
before collection: first4 training, episode4 validation, final2 held-out test.
Do not inspect or evaluate test trajectories during training/model selection.
All seven are novel simulator seeds; none are the L4 development seed.

Collection runs synchronously without real-time claims. Generate50-action chunks
with the frozen policy; execute actual postprocessed actions without clipping.
Encode each observed current image exactly once and reuse its native token
override for generation at chunk boundaries. A same-noise native-RGB/override
comparison on the first observation must agree before proceeding. Keep original
token dtype/masks, normalized model-ready32state, normalized6actions and actual
chunk IDs. Save complete episode tensors and outcomes only after episode control
has stopped. Any native terminal excludes its already-reset observation from
future targets and ends that episode; no retry with another seed.

For sample(t,d), d=1..8, target is actual Z[t+d]. Input is currentZ[t], current
model-ready state and the action prefix A[t:t+d] in the **saved policy-normalized
space before postprocessing**. Require all these actions to have been generated
in the same already-committed chunk at time t. Exclude chunk-boundary crossings,
so future-observation-generated actions never leak into predictor input. No
future state, image, task outcome or test statistic enters the predictor.

## Fixed training and selection

Reuse `LightweightFutureLatentPredictor`, newly initialized with seed1907:
native observed token_dim, action_dim6, state_dim32, rank64, action_hidden128,
state_hidden64, delay_embedding32, fusion_hidden128, max_cameras2, horizon8,
depthwise1d mixer, **risk_head=false**. Count parameters in the result.

Train20epochs, batch32, AdamW lr0.001/weight_decay0.0001, gradient clipping1.
Loss: masked mean SmoothL1(predictedZ,targetZ) +0.1*masked mean cosine distance.
Delay/timestep examples are shuffled with the fixed training seed; no test access.
Select the checkpoint with lowest aggregate validation SmoothL1, tie earliest.
The identity baseline is Z[t], evaluated on exactly the same valid targets/masks.

After selection is frozen, evaluate **held-out test once**, reporting per-episode
and per-delay SmoothL1/cosine for identity and predictor, sample counts, absolute
errors and relative changes. Negative improvements remain visible. No new risk
threshold, fallback, checkpoint tuning or statistical-significance claim follows
from these two test episodes. A pass for this pilot requires positive aggregate
test SmoothL1 reduction and no hidden omission of adverse delays/episodes; this
does not replace the project's old scientific gates or qualify online deployment.

Artifacts are a new independent L6 directory with source commits, all seeds,
candidate/collection configuration, true outcomes, masks, splits, training log,
selected checkpoint and test report. Publish small reports/source to GitHub;
keep large latent caches and model artifacts locally. Do not push model weights
or trajectories to a public dataset repository. Old candidate validators stay
unchanged; production `predicted` remains disabled for this new candidate until
a separate explicit runtime binding and relevant validation are implemented.
