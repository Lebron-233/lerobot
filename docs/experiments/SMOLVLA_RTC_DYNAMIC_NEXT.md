# E-RDC1 / E-RDF1 接续：动态前缀接入已完成，任务保持仍待解决

2026-09-22。执行fa80d0b5。两段实验和独立审计已结束，禁止重跑原唯一输出。动态组合225/225等价，真实RTC反馈8/10，两个任务保持开发门失败。旧无RTC冻结基线及原独立确认阴性保持；用户要求的原动作时间对齐和实际消费一次trim不改变。

## 当前应停止的循环

不要再重复实现前缀传入或再次测固定delay3速度；它们已有证据。不要修改起始行来消除7/18失败，也不要仅展示7/19恢复。不要把前缀贴合改善当作任务保持。两阶段未使用新独立确认身份，不构成生产资格。

## 下一项可检验问题，而非已证明原因

本轮79次RTC接管中78次预计delay大于实际消费，初次预计7而实际3或4。预计delay原本带计算覆盖余量，却也决定RTC强约束前缀。这个事实支持检查“计算预算估计”和“实际承诺执行范围”是否应分开表示；它没有证明过约束是唯一失败原因，不能据单例立即搜索引导强度、时域长度或延迟门限。

候选假设：在提交时明确承诺旧动作前缀的长度，让生成时的前缀条件与实际承诺范围一致，同时保持原观测原点和一次trim。保留旧RTC作为参照；任何新安装策略另版本，不修改fa80d0b5的既有结果。

## 建议执行顺序（本次未实现、未登记、未运行）

先做合成时序测试：结果提前、按时、迟到，承诺范围耗尽，实际延迟超过cap，episode提前结束。明确区分预计计算覆盖、声明承诺量、已执行量、结果起始行四个量。不得用推理结束后才知道的实际delay作为生成前的线上输入，或读取未来观测。

若研究“结果提前完成仍执行已承诺前缀”的候选，应在新合同中明确早到安装时刻和迟到处置；迟到等待、丢弃、无动作时隙全部计数，不允许把人为等待隐为实时通过。原trim始终以实际消费量计算。只允许一个预先选择的接管合同，不同时搜索多套长度或权重后挑最好者。

合成校验通过后，在同一完整计算公式与权重、50行/十步/20Hz/cap8/350ms预算下，另立有界同期开发对照。保留全部10个开发身份，尤其2/11、7/18、7/19；不得仅在失败身份设置特例。运行前确定比较臂、顺序、次数、预算和无丢失成功的判据；不自动把现有合同扩充为新试验。

必须同时报告声明承诺与实际消费、旧前缀来源、完整请求与提交至完成时延、早到/迟到/等待、全部任务结果及不利配对。出现任务保持失败即保留阴性，不用贴合误差或消除等待抵消失败。

开发候选成立后才能重新冻结，在查重后的未用身份上另行独立确认。原32对不能再次标为未见数据；不放宽旧置信界、不拼入旧先导或追加到阳性。没有授权真机、长期无人看管运行、依赖升级或生产默认变化。

## 精确断点

已完成：E-RDC1输入审计、E-RDF1反馈审计与全部结果保存；新模型前向和环境在本次接续均为0。原worker均已退出，没有待续运行任务。首先阅读SMOLVLA_RTC_DYNAMIC_RESULT.md及两段原audit；不要重复prepare、提交登记或启动旧worker。

原21份pending继续保留。必要发布已有用户授权；明确安全权限拒绝停止对应动作，不以换工具规避；普通网络故障先核实实际状态，未知POST不盲目重发。结果提交和实际评论状态以原输出目录publication_receipt.json为准。
