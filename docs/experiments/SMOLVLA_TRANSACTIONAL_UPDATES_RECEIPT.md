# F-TUA1执行回执：完整回退可用，严格训练门未带来开发验证改善

2026-09-17（Asia/Tokyo）。本轮真实执行72次候选更新和5184次全训练集检查，独立审计通过；不是新的部署成功。

## 身份、登记与退出

execution/code HEAD：dc07ccb58a88fd1c7f29708d9fa248d8a3554200。
分支：codex/smolvla-graph-native-equivalence。
预登记：https://github.com/Lebron-233/lerobot/issues/1#issuecomment-5706970273。
唯一OUT：/home/rp/Workspace/SmolVLA_RTC/lerobot/outputs/smolvla_transactional_updates_dc07ccb5。
preparation.json SHA256：c4ce62c1f196b57cee43422e03f1c030c88424deabc1efe86602ce33a9ed6c43。
登记正文SHA256：b6bfd4ee6fe6061cd771d52358774882a45d09d47f45a2903fb63161714e50a0。
单次独立gh POST、按实际ID GET、body exact已在运行前完成。保存的六字段JSON是实际返回的规范化投影，不是原始HTTP字节捕获。

正式Job：a3d843d0-601e-44c1-bf73-20be9a0d6d09，completed/exit0，duration_ms=961966，active=false。
worker655136 exit0，监督器exit_confirmed=true；UTC01:22:35.121294至01:38:32.946317，wall=957.8250378409866秒。
first_failure/stop_reason=null，pending/active为空，无强制终止；attempt1/retry0。拒绝提案不是正式worker重试。
审计Job：4da799a0-8180-4046-96ba-d6590ba09d4c，completed/exit0，内部wall=7.4429418579966296秒。

## 不变设计与唯一实验变量

从原F-ACR1 centered第72步重新初始化，69680参数。I+(h(a)-h(0))幅度1，原BF16边界，原BRP目标和训练参照。
同72训练/18episode、原顺序、AdamW lr1e-4/wd1e-4/betas0.9,0.999/eps1e-8/foreach=false/clip1。
每例一次提案，不搜索步长。候选后完整评估全部72训练样本：目标均值严格下降、首动作/chunk均值和相对Identity最坏超额不升，且后三量不劣初始，才接受。
拒绝时逐值恢复权重和optimizer全部moment/step/参数组。回退后下一次优化的历史只包括已接受更新。
16开发验证仅初始已知输出exact和最终评估，不用于提案接受/选择。旧R1资格32样本继续封存。
历史BRP/guarded/plain不是本轮同期重训对照。不同接受数量导致参数和优化器路径改变，不能归因于某一个旧更新被阻止。

## 有意义的结论与明确失败

independent_contract_accepted=true。
transaction_mechanism_completed=true。
development_followup_supported=false。
independent_qualification_claimed=false；deployment_qualified=false。

72次提案只接受1、4、29，共3次，拒绝69次（95.833333%）。第29提案后43次全部回退，最终模型保持在第三次接受状态。
所有69次拒绝的权重、动量和step恢复均通过独立审计。最终训练预测与最后接受扫描逐值一致，非假保存或仅恢复权重。
但训练门通过是算法约束的直接结果，不能单独解释为泛化提升。

| 训练集量 | 初始冻结IAR | 最终transaction | 相对降低 |
|---|---:|---:|---:|
| 完整BRP目标 | 2.479002743621 | 2.446756854263 | 1.300761% |
| 首动作7D MSE | 0.053600007189 | 0.053117466641 | 0.900262% |
| 50x7D chunk MSE | 0.082743250036 | 0.080813704719 | 2.331967% |
| 相对Identity最大正超额首动作 | 0.642751500463 | 0.633900032891 | 1.377121% |

原72例训练首动作相对冻结候选46例改善/26例恶化，12/18episode改善；全部18种留一episode平均收益为正。聚合门不保证每例无退化。

| 开发验证方案 | 首动作7D MSE | chunk MSE |
|---|---:|---:|
| Identity | 0.031709702433 | 0.053248021229 |
| 冻结IAR | 0.012048372598 | 0.026728824581 |
| 历史plain | 0.015780598674 | 0.031444426151 |
| 历史guarded | 0.015590678394 | 0.028277092513 |
| 历史BRP | 0.014130978751 | 0.028661323583 |
| 本轮transaction | 0.012577338758 | 0.027514487743 |

transaction验证首动作/chunk比冻结IAR分别增加4.390354%/2.939385%。首动作5/16样本改善、11/16恶化；0/4episode改善，四种留一episode收益也全负。
虽然相对历史BRP两项误差降低10.994567%/4.001336%，这只代表比该退化微调方案更接近冻结起点，不能称为超过冻结候选。
相对Identity首动作低60.335992%，但冻结IAR已有更好的表现；不得用弱基线掩盖实际退化。
相对自身错配首动作只有6/16样本改善、10/16恶化，尽管4/4episode均值改善；保留完整配对结果。

13项固定开发条件中9项满足、4项不满足：首动作不劣冻结、chunk优于冻结、训练最坏超额优于guarded、训练最坏超额优于plain均false。
没有改选其他候选，没有放宽门，也没有用验证指标决定接受过程。

## 真实消耗与独立审计

| 项目 | 实际 |
|---|---:|
| VLA/预测器实例加载 | 1/1 |
| AdamW提案/反传 | 72/72 |
| 保留/回退更新 | 3/69 |
| 正式decoder/预测器前向 | 5781/11210 |
| 训练门样本前向 | 5184 |
| 原I/旧B+delta/冻结IAR完整重放 | 各88 exact |
| zero raw/完整输出 | 88 exact |
| Graph capture | 448 |
| capture内部setup/warmup/capture | 448/1344/448，另列 |
| 独立CPU数值比较 | 17415 |
| AdamW参数数组核验 | 1512 |
| phase started/returned | 5857/5857 |
| 新Env/图像编码/资格读取/真机 | 全部0 |

训练扫描和回退的已消耗计算没有被记成0。此前ITC/BRP/UDI等成本保留；本轮是新增72次提案，其中仅3次成为最终优化器历史。
完整运行957.83秒，明显存在全训练检查开销；不是在线推理延迟、受控硬件性能对照或闭环速度。
审计CPU/CUDA_initialized=false、模型前向0；不独立求导。验证保存梯度的clip和AdamW更新算术、全部扫描指标及门决策；不声称独立重算整个自动微分。
83项CPU回归、Ruff通过；合成CUDA回退检查通过。该合成检查初始化CUDA但未读科学样本或模型。
112份来源和21份原pending状态/字节在准备、执行、审计中保持。没有更改依赖、驱动或配置。

## 安全拦截

正式运行期间，一次附加进度统计请求被拦截，原始错误：

> 因 OpenAI 无法确定请求的安全状态，已拦截此工具调用。

该请求没有输出、PID或退出回执，不计为成功证据。没有重发或换工具重做该进度统计。
之后只用原Job的生命周期接口监督；正式Job自行结束后，执行了原定独立审计和最终报告读取。它们不是重新启动实验或补做被拒绝的进度统计。

## 后继判断

本轮实现了完整可审核的有限步回退，并说明该固定方案的局限：全训练集聚合约束能保住训练集量，却不能保证四个开发验证episode的收益；极低接受率且扫描成本高。
单样本梯度提案与全训练集多指标接受条件不匹配是值得检验的假设，不是本轮已经独立证明的唯一原因。
未来需要匹配有效更新次数的无门对照，并检验与聚合检查一致的提案方向；不应只扩大训练轮数或放宽约束到通过。
用于部署推进的性能参照仍应保留冻结IAR；本轮transaction不升级资格、不进入时延或闭环。
本轮没有宣布完成预测式SmolVLA异步推理。旧R1阴性保持，risk_thresholds=null。

本回执与全部结果将按有意义的已审计机制/阴性验证证据整理，实际出版HEAD和评论ID由之后独立发布回执记录，不预造。
