# F-SCL1-r1正式结果：尺度调整改善开发均值，动作输入与低误差保护优势仍未成立

2026-09-10。冻结执行HEAD `6ddb7505c4d1c5930db365a818c2bc1d2e47b2d4`；预登记5615880107。
本报告补全状态评论5616007958的独立核验断点，不是第二次训练。
本次只执行保存数组的CPU核验、数值整理和发布；新增模型前向、优化更新、编码、Env/native及test读取均0。
原F-SCL1数据门失败、r1运行结果和audit_first失败日志均保留，未覆盖原数值或重新选择检查点。

## 一、审计断点处理及接纳依据

原审计把准备与运行的整个尺度字典要求逐值相等。冻结PLAN对计算归约的标准是rtol=1e-6、atol=1e-7，
而token/动作/来源身份仍要求exact。本次仅对计算所得的`/scales/latent`应用原定归约标准：

| 项目 | 数值 |
|---|---:|
| CPU准备token尺度 | 2131.5723928897937 |
| 实际训练使用token尺度 | 2131.5723863966923 |
| 绝对差 | 0.000006493101409432711 |
| 相对差 | 3.046155716e-9 |

把这一项从字典比较中单独取出后，其余所有字段仍要求逐值相等并通过：72个案例权重、
动作尺度、样本键、floor、低误差阈值和其他元数据没有变化。实际训练没有使用准备值替换运行值。
没有放宽输入或动作exact门，也没有再次执行被拒的线程来源查询；不声称确认了差异的唯一根因。

修订审计标签为`audit_final_reduction_contract`，UTC09:14:57.592509启动，外层4.444252406秒、exit0。
最终`independent_contract_accepted=true`，2558项数值比较通过；最大相对归约差2.125619431e-7。
原`audit_first`的AssertionError/exit1和旧STATUS/状态发布回执保留，不能把旧状态回执当作本次最终接纳回执。

## 二、数据、四组与训练约束

共同训练72例/18episode：task0–5的state46/48/49，每episode按已冻结规则取4例。
共同验证16例/4episode：task6–7的state48/49；这些验证样本已经用于开发，不是独立测试。
训练真实延迟为69例delay3、3例delay4；验证16例均delay3，未删除或改写四步样本。
3/48/6、3/49/6、4/48/6三例原生来源核对在上一阶段完成，本次只复用已保存source_trace及源文件hash，未重复扫描原轨迹。
原task8/9评估数据及confirmation21–40不参与本次计算或选点。

四组global_conditioned、case_conditioned、global_no_action、case_no_action均为69680参数、
同seed20260912、零残差初始化、72updates/batch1、AdamW lr0.001/wd0.0001/clip1。
样本顺序一致，每个训练样本恰好一次；VLA冻结，原十步采样器，当前model-ready32Dstate和normalized7D动作语义不变。
无动作组只把动作值置零；mask、delay、视觉、state、语言、噪声保持。

global目标为Lz/Sz+La/Sa；case目标为Lz/Sz+wi*La/Sa。
Sa=0.058981226729922634；Ei为训练例identity首动作到已有oracle的误差。
floor=max(0.1*Sa,1e-12)=0.005898122672992264；qi=Sa/max(Ei,floor)，wi=qi/mean_train(q)。
权重均值1，范围0.009588146909267625至1.7432879944691861；没有扫描floor或加入其他损失项。
低identity误差阈值仅取训练Ei的25%分位数，为0.002958781563678625；不用验证误差调阈值。

四组都使用validation原始首动作episode-macro，在0/36/72步选最早最小值，包含identity起点。
两组global选择72步，两组case选择36步；四个所选检查点均非零。
总训练预算相同，所选步数不同；以下是完整“训练＋验证选点”流程的比较，不伪装成同一步数的直接损失效应。

## 三、完整验证指标

四episode等权，越低越好。identity由原保存输出CPU float64复算，模型均值来自已核验运行记录；
因此末位可能与旧GPU float32报告不同，不改变判据或方向。

| 方法 | 所选步数 | token MSE | 首动作MSE | 完整50行动作块MSE |
|---|---:|---:|---:|---:|
| identity | — | 2670.828930525 | 0.031709702433 | 0.053248021229 |
| global_conditioned | 72 | 2674.252922058 | 0.029442513158 | 0.049140438008 |
| case_conditioned | 36 | 2671.549179077 | 0.028754210696 | 0.039587857125 |
| global_no_action | 72 | 2674.301811218 | 0.029710716241 | 0.049999032271 |
| case_no_action | 36 | 2671.641105652 | 0.028710898987 | 0.039825450243 |

case_conditioned相对identity首动作误差低9.320465%、整块低25.653844%，token误差高0.026967%。
相对global_conditioned首动作/整块低2.337784%/19.439348%；相对global_no_action低3.219396%/20.822753%。
但是相对case_no_action，首动作误差高0.150855%，整块低0.596586%。
首动作最优是case_no_action，整块最优是case_conditioned；不能把尺度改善写成动作输入增量。

`scale_row0_improved=true`、`nonidentity_selected=true`；
`action_increment_observed=false`、`development_candidate_gate_passed=false`、`all_row0_comparators_better=false`。
主门失败项是case_conditioned的首动作未严格优于case_no_action；不是程序失败或选回identity。

### 同一步数的补充比较（仅使用已保存曲线，不重新选点）

| 固定步数 | global有动作首动作 | case有动作首动作 | global有动作整块 | case有动作整块 |
|---:|---:|---:|---:|---:|
| 36 | 0.032244153379 | 0.028754210696 | 0.030884905544 | 0.039587857125 |
| 72 | 0.029442513158 | 0.029274066655 | 0.049140438008 | 0.048418944647 |

36步时case首动作较好、整块较差；72步时两项均略好。所选检查点整块改善19.44%，
不能泛化为每个训练阶段都改善同样幅度，也不能据此改回其他检查点。

### 逐验证episode的首动作

| task/state | identity | global有动作 | case有动作 | global无动作 | case无动作 |
|---|---:|---:|---:|---:|---:|
| 6/48 | 0.030254651276 | 0.039607416664 | 0.031347810116 | 0.039334152068 | 0.030766268203 |
| 6/49 | 0.016374575198 | 0.012830489228 | 0.009937146126 | 0.012694875913 | 0.010323022725 |
| 7/48 | 0.013116090748 | 0.012457482633 | 0.008532909909 | 0.012669779127 | 0.008131540846 |
| 7/49 | 0.067093492511 | 0.052874664107 | 0.065198976634 | 0.054144057853 | 0.065622764174 |

四组各3/4条优于identity。case有动作在6/48仍差3.613193%，虽然比global在此条的退化小；
在7/49反而不如global有动作。不能只报告总体正均值。

## 四、低误差样本保护：训练改善，验证未确认

所有子群均使用同一个训练阈值，不由所选模型或结果筛样；harmed按既定归约容差之外的误差增加计数。

| 群体 | identity均值 | global有动作 | case有动作 | global无动作 | case无动作 |
|---|---:|---:|---:|---:|---:|
| 训练低误差18例：首动作MSE | 0.001653887817 | 0.003161149753 | 0.001536748563 | 0.003069178010 | 0.001720339471 |
| 训练低误差18例：受损数 | — | 13/18 | 9/18 | 13/18 | 10/18 |
| 验证低误差1例：首动作MSE | 0.001064171039 | 0.002864211565 | 0.002971239155 | 0.003429981647 | 0.002393726958 |
| 验证低误差1例：受损数 | — | 1/1 | 1/1 | 1/1 | 1/1 |

case有动作的训练低误差均值比identity低，受损数从global的13/18降到9/18；仍有一半样本受损。
验证唯一低误差样本增加0.001907068116，且比global有动作的输出误差还高0.000107027590。
这不足以估计一般性的保护率，更不能宣布低误差保护迁移成功。没有按这个样本调门控或删例。

共同72例训练总体首动作MSE：identity 0.075968549259；四组依次0.061493920078、0.063499498015、
0.061717294586、0.064137493780。case降低了低误差子群的损害，但训练总体误差不如global；
它是开发目标的取舍，不是所有训练／验证指标全面领先。

## 五、账目、来源与退出

原运行正式decoder812=参考44+验证192+梯度训练288+所选训练集评估288；
gradient_decoder/backward/updates各288；Graph捕获56，内部setup/warmup/capture为56/168/56，另记不冒充额外训练样本。
44个运行时参考重放exact；退出审计直接回读64个零步验证完整输出，与原生归档逐值exact。
2558项检查包含权重、实际损失计算、固定顺序、验证选点、共同训练/验证三项指标和低误差子群。
两份开发标签SHA256不变，同初始化逐tensor exact；没有修改来源或已保存权重，VLA始终冻结。
1107组外层/离线phase完整，错误/未知/超时0；Graph释放确认，环境Python/version/140包一致。

原child2954527与supervisor2954439 exit0且收回，无强制终止。
原UTC08:52:35.725967至08:58:08.245056，监督328.503070405秒、独立外层332.519130803秒。
本次审计内部0.484671821秒，外层4.444252406秒；不把管线总耗时写成预测器延迟。
attempt1/retry0保持，本次新forward/训练/Env/native/编码/test均0。

## 六、结论和接续

尺度重加权在已有开发验证上改善了首动作与整块均值，训练低误差群体受损减少；
动作输入增量仍未建立，验证低误差保护也未确认。全局两组的记录与此前F-COV1对应多初态组相符，
但本次不重跑旧训练或将各轮分母拼接成新测试。
单训练seed、18个训练episode/4个开发验证episode、每条前4例且多次开发复用，结论不能升级为独立泛化。
oracle仍是实际future视觉+当前state+同语言/noise的策略输出，不是专家/最优动作或闭环success上界。

下一阶段回到训练/验证内的动作信息有效性开发问题，保留无动作基线；先单独定义新假设再开展实验，
不消耗新test追逐正结果，不在这次负结果后调残差强度、floor或改判据。详见本轮NEXT。
生产默认、baseline/realtime/predictor闭环资格false、risk_thresholds=null及confirmation untouched保持。

原件：`outputs/smolvla_case_scale_r1_6ddb7505/`，最终`independent_audit.json`和`report_data.json`已落盘。
审计器与回执：`outputs/smolvla_case_scale_r1_preparation_eae8c863/`；
本报告和机器结果提交后单独发布评论并exact回读，最终结果发布回执与旧STATUS回执分开。
