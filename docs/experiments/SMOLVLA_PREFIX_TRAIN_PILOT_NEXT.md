# F-PTP1精确接续：冻结两份小试检查点，不把同条件收益当作晋级

已完成：F-DTC1两种频率数据回放和CPU审计；首4RLDS分片的整轨迹cohort冻结/审计；F-PTP1普通与前缀投影微调各128更新、144评价和独立审计。禁止重跑这些唯一输出、补样或追加当前训练步数到阳性。

## 当前事实和路线判断

相同正确前缀条件下，prefix末态比ordinary低3.504%，四条dev都改善；但prefix正确条件0.330983282仍高于ordinary无条件0.323490898，主比较仅1/4轨迹改善，offline_followup_supported=false。旧Graph/真实反馈/原时间对齐继续保留；新投影权重不替换base、不进入闭环或独立资格。不能把0.33流速度损失解释成最终动作误差、任务成功率或硬实时资格。

本轮主要得到“该训练输入形式可以通过真实优化改善”，而不是已证明前缀模型总体优于无条件。只有723392个投影参数更新，不能据此认定完整专家训练必然有效或整个路线失败。不要扫描全局mask/C/强度，或直接靠增加迭代次数掩盖主比较失败。

## 后继最小问题（尚未实现/运行）

先在**不再更新权重**的前提下，补齐与训练一致的条件采样语义，并限定为离线动作诊断。它不是已经晋级的闭环候选：需新固定合同说明是在检验流速度损失与实际动作误差是否一致，而不是放宽本轮门。

条件采样要求：接收请求创建时已声明的归一化旧动作前缀、固定噪声和C。每个积分步前C行保持干净并使用逐动作time0；后缀按原十步Euler时间生成，不用后见的actual delay或专家未来状态做线上输入。C0应与同检查点的原始无前缀完整50x32输出逐值一致；有效7D与32D padding必须明确，不能静默开trained RTC配置或修改旧接口校验。

先做合成积分、逐动作时间、padding、C0退化和checkpoint reload测试。加载独立projection checkpoint到原始权重副本，base revision和normalizer必须匹配；普通检查点不得被误称为前缀训练检查点。所有旧生产文件保持。

通过后，另行预登记固定的**旧dev离线十步解码诊断**：原冻结、ordinary末态、prefix末态；对比相同接收者/噪声/合法后缀下的完整动作MSE、首个将执行动作误差、正确/错配条件。保留全部四轨迹和所有不利样本；数据是已见dev，不读4sealed、不宣称独立确认。没有用整块平均改善覆盖接管首动作退化。

如果解码动作仍不优于简单参照，就继续保留阴性，先判断数据覆盖/模型可训练部分的不足，再决定是否另立同预算更充分训练；不自动增大网络或训练量。只有新的动作质量与接口合同成立，才讨论新检查点eager/Graph等价、完整时延和真实异步闭环；旧Graph数值资格不自动转移到新权重。

## 冻结原件

微调执行：24c7ed49c6f05299991e605b7b92a21721c69e09。
原件：outputs/smolvla_prefix_train_pilot_24c7ed49。
ordinary_checkpoint.pt SHA2377b2c3d849c909f9cdf5b320e6d97b6fab07008f96541c5b86f62a41e77ac9。
prefix_checkpoint.pt SHA0d424426bd2fa9a2f0f4abb69b4e10c3443a2029749366bf16f2cca9821c2bed。
两份保存投影参数的完整数值及optimizer状态，不是完整模型，也不是可相加的数值delta；需要明确base来源和新条件采样器，deployment_ready=false。原始模型参数已恢复。

cohort manifest d1a60540fce2056d10c29ea361329da2730e2e5f89415f86731c9e57c68d7bbf，12train/4dev/4sealed。sealed的结构/数值hash曾用于完整性核查，图像没有解码、没有模型评价；不能声称文件完全未触及。源基座预训练是否见过这些演示未知。

本轮source物理频率诊断仅一条演示，加上固定上游生成规则支持20Hz行解释，不保证所有任务物理状态逐值可复现。源图像与转换JPEG有17/20像素差，本轮使用原始RLDS一体数据，不允许随意混源或改fps标签。

用户必要预登记、原分支push和Issue1结果发布授权沿用。明确安全拒绝停止对应动作，普通网络失败先核实真实状态。旧21pending、旧32对确认阴性和全部RTC不利结果不动。无真机、生产默认修改或长期无人看管任务。
